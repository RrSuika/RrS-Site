# AI_CONTEXT.md — RrSuika Studio Portfolio 项目上下文（AI 专用）

> **本文件是给未来 AI 助手的项目级长期记忆（Architecture & Design Memory）。**
> 接手任何新任务时：**先读本文件 → 再用快速索引（§11）定位文件 → 只读必要的源文件 → 动手。**
> 与 `CLAUDE.md` 的分工：CLAUDE.md 是**规则与政策**（必须遵守的约束），本文件是**架构事实与原因**（"现在怎么组织的、为什么、去哪改"）。`AGENTS.md` 只讲开发服务器的工作方式。
>
> - 2026-09-18 v1.15（**projects / lab 卡片 → 磁带盒书架**：新增 `src/components/CassetteShelf.astro`，六个列表页（en/zh/nl × projects/lab）的卡片网格整体换成「档案架上的磁带盒」——封面沿用原卡片图，选中那盘被抽出并**旋转 15°**，露出**左侧书脊 + 底部厚度**并独占暖光，其余磁带在 Z 轴后退、顶部向中心倾斜形成倒三角构图；右侧面板显示当前磁带的简介，点击带过场进入详情页，左右拖动＝磁带转为侧面并放回书架（超过 `max(92px, 6.5vw)` 才换下一盘，未超过则回弹）。参考文件 `输入/磁带盒启动动画.html` + `输入/磁带盒主体动画.html` **仅作参考**（three.js + CDN）：实现走 **CSS 3D `preserve-3d`**，不引依赖、封面仍走 astro:assets、文案仍是真 DOM（三语/SEO/无障碍都保住），世界的姿态数学则 1:1 移植过来。⚠️ 最大陷阱是 **preserve-3d 卫生**：`.cas-f` 面的任何祖先一旦获得 `overflow:hidden` / `opacity<1` / `filter` / `backdrop-filter` / `clip-path` / `mask` / 非 3D `transform`，`transform-style` 会被强制降为 `flat`，整盒塌成平面卡——所以逐盘淡入淡出走 `--cas-o` 变量、由**面**消费，绝不加在磁带元素上。符号约定：参考的 Three.js 欧拉角 → CSS 为 `translate3d(x,-y,z) rotateX(-rx) rotateY(ry) rotateZ(-rz)`；主角 `ry = +15°`（参考是 `-15°`，露的是**右**侧壁；用户要的是左侧）。**≤860px / `prefers-reduced-motion` / 不支持 `preserve-3d` ⇒ flat 模式**（`.shelf-page[data-mode="flat"]`，同一套 markUp 与状态机，纯平面卡片切换、不做 3D、不放升架动画）。完整开场**每会话只播一次**（`sessionStorage["rrsuika-shelf-intro-seen"]`），REPLAY INTRO 与标签筛选始终重播。见 §5.2、§9 决策 23、§11）
> - 2026-09-18 v1.16（**磁带盒书架第二轮：七盘满屏 + 边缘不跳变 + 封满封面 + 换语言续看当前盘**）：① **一屏七盘**——`SPREAD_X 3.42→3.4`、`MIN_WORLD_W 9.4→26`、`WORLD_H 5.6→4.3`、外壳深度 `0.70→0.37`（约 W 的 7.6%，之前的 VHS 厚砖在七盘排布下会互相穿插）；**行宽由窗口宽度决定而不是高度**（"一屏七盘"是横向承诺，所以 `S = min(h/WORLD_H, w/MIN_WORLD_W)` 里宽度这一项必须赢，否则窄窗口下磁带会缩到七盘只占屏幕 60%）。实测 1600×900 主角 329px、七盘全在画面内。② **边缘不再跳变**——这是本轮最大的重构。旧实现按「9 盘窗口 + 按行长度取模循环」定位，取模会让**停车位别名**（偏移 7 读回 −5），于是停在场外的磁带被画在活磁带上、用户看到最右一张突然消失、最左一张闪一下。现在只有**一个**位置公式 `slotIndex(tape) = ((ring − current + n + ⌊n/2⌋) % n) − ⌊n/2⌋`（对称取模——左侧邻居在环上是**负数**，用普通 `%` 会报成 +7/+8/+9 然后被当作业外裁掉），盘位、淡入淡出、排序三处都读它；停车位是 `±parkSlots()`，永远落在画框外，且**按符号停在同一侧**，所以回来的那盘本来就在那一侧、无需横穿画面。副产物：一度出现「选中环画在 06、右侧简介写 01」正是三份派生拷贝漂移所致，一条公式从根上消除。③ **封面铺满整个盒面**——去掉底部 deck 条，`.cas-label` 改成 `inset: 0` 铺满，转盘/编号/日期/色条/标题全部浮在封面上（转盘带半透明深色底盘，像透过透明外壳看 J-card）；标题换 **`--font-display`（Saira Condensed 700）**——JetBrains Mono 900 在这个尺寸下就是一片方块。④ **`INSPECT FILE` 变成真按钮**：`.shelf-open` 实心强调色板 + 右侧箭头，走**和点击磁带同一条** `launch()`，不是 `<a>` 直跳。⑤ **WIP 变琥珀**：状态点由 CSS 盒模型画（不再依赖文案里的 `●`/`◐` 字形——那个字形会在窄面板里被折行拆到上一行，正是 Zoembike 卡「绿色点被截成两行」的原因），`data-wip="1"` 时整块转 `--stripe-yellow` 且点变半圆；状态文案在渲染期用正则剥掉遗留字形，共享翻译表不动（`ProjectCard` 仍原样使用）。`ProjectCard.status` 顺手加 `white-space: nowrap` 防同类折行。⑥ **拖动不再变成拖图片/拖链接**：`draggable="false"` + `-webkit-user-drag:none` + `user-select:none` + `dragstart` preventDefault。⑦ **tags 筛选不再黑屏**：筛选**不再重播开场**（`startIntro` 不再被调用），只把存活的磁带滑到新盘位、被筛掉的在原地淡出；封面预热改成**进入网站时一次**（`sessionStorage["rrsuika-shelf-warm"]` 门控 `#shelf-warm` 的内联脚本注入 `<link rel=preload>`）——⚠️ 原实现用 `Astro.session` 判定，而本项目 `output: "static"` **没有 adapter**，`Astro.session` 根本不存在，判定恒为 false，于是每次翻页都重发 12 条 preload 并重播黑幕；换成 sessionStorage 后第二次进入实测 preload 数 0、开场立即揭幕。⑧ **换语言续看当前盘**：`sessionStorage["rrsuika-shelf-card"]` 记录 `current`，语言切换=整页跳转，落页时读回并**不重播开场**（同 `warm` 门控），实测「第 3 盘 → 切中文」仍然停在第 3 盘。⑨ **箭头改 Q 版加粗**：圆胶囊底 + 两根 12×3.5px 圆角短棒拼的 chevron（细 `border` 折角在这个尺寸下没有重量），悬停填强调色，Q 弹在短棒上跑 `translate/scale`（不再和 `rotate` 抢 `transform`）。⑩ **拖动释放**：`pointermove` 采样最近 100ms 的真实位移换算成 progress/s（单帧差太抖），超过 `FLICK_VELOCITY` 才算甩动，否则按距离阈值 `max(92px, 6.5vw)` 决定前进还是回弹；回弹用 RhineLabUI 的临界阻尼 `damp()`（保留速度）。⑪ 主题切换**圆形揭示**：View Transitions + `clip-path: circle()` 从点击处扩到最远角，`html.theme-reveal` 下强制两层快照不淡入淡出只动画新层（否则和默认 cross-fade 打架），无 VT / reduced-motion 直接瞬切，`finished` + 1.4s 兜底清 class。见 §5.2、§9 决策 23、§11）

> - 2026-09-18 v1.17（**磁带盒书架第三轮：真正居中 + 空磁带填满两侧 + 单条缓动换盘 + 入场不僵 + 箭头方向修正**）：用户对着渲染结果逐条提意见，全部按参考文件（`输入/磁带盒启动动画.html`、`输入/磁带盒主体动画.html`，**这两份文件现在确实在工作区里**，此前 v1.15 的笔记说它们不在树上是错的）重做。① **居中**——这是本轮的真 bug：绘制带是 `current ± half`，所以只有把选中盘**正好放在环上第 half 个位置**，主角才落在屏幕正中；原来的实现把选中盘放在「第一张真磁带」上，整行因此左移半屏。五张项目的架子实测 `heroOff` 从 −295px 变成 **−8px**。② **空磁带填两侧**——新增 12 个 `.cas-blank`（同款外壳、无链接无文案、`aria-hidden`），角色是**布景**：`buildRing()` 把过滤结果补到 `2·MAX_SLOTS+1` 满行，`walkFrom/walkTo` 只覆盖真磁带，所以空白永远不会被选中（`navigate`/`recycle` 越界时改用临界阻尼弹回，而不是卡在半路）。`MAX_SLOTS 3→6`，一屏变成 **13 个绘制位**（7 个完整 + 两侧渐隐），1600px 下 5 个项目也不空了。⚠️ `casEls` 必须用 `:not([data-fillers] .cas)` 排除空盘——直接 `querySelectorAll(".cas")` 会把 12 个布景当成真项目（真实踩到：总数显示 17、环里全是 R）。③ **换盘丝滑**——删掉「每盘各自指数逼近自己的槽位」，改成**一条缓动 tween 驱动全行**（`progress` 是所有盘共享的连续量，`slotIndex = ring − current + progress`），落地时环再跳一格；实测 45 帧内零反向、delta 单调衰减 `-49 -38 -27 -19 -13 -9 -4 -3 -1 0`。④ **入场不僵**——照抄参考的升架：X 轴**锁死在自己槽位上**（旧实现在升架时把 X 从架子位插值到主角位，横向滑过画面，这就是「僵硬」的来源）、全程**侧身**（ry = ±90°，参考的 `sideAngle`）、scale 2.05→2.30。⑤ **箭头**——旧实现用「一个方块 + 两条 border + 单次 rotate」，结果两个箭头都指向右。现在改成**两根粗圆棒拼成的 V 形**，源码里就是右向箭头，左向靠 `scaleX(-1)` 镜像：方向只由一处决定。同时**去掉圆框**（只要箭头），Q 弹跑在内层（避免 `translate/scale` 和镜像/旋转抢 `transform`）。实测 DOM 方向判定与截图像素重心两边都通过。⑥ **删掉左侧竖条**（`shelf-rail`：整行 `// TAPE LIBRARY` + 竖线 + 序号）标记、样式、flat 模式规则、i18n 引用一并清掉。⑦ 同轮修掉一个 **PowerShell 编码事故**：用 `Set-Content` 回写该文件时没有指定编码，把全部 3 字节字符（`—`、`≈`、`≤`、`：`、`●`、CJK 文件名、`═` 分隔线）打坏成 `U+FFFD`，产生 843 个类型错误；已按上下文逐类还原（emoji/破折号/CJK 文件名/分隔线），并复原被吃掉换行的分节注释。⑧ 另：`realEls` 排除空盘后 `total` 回到真实条数（5 / 12）。见 §5.2、§9 决策 23、§11）
> - 2026-09-18 v1.18（**磁带盒书架第四轮：按参考动画重做动效节奏 + 不透明冷色 + 九盘密排 + 箭头闭合**）：这一轮用户给了三条可执行的线索，全部照做。① **动效照抄 `磁带盒主体动画.html`**（该文件一直就在 `输入/`，v1.15 笔记说它不在树上是错的）：导航时长用参考原值 `progress===0 ? 620 : 380+distance·180`，拖拽越阈 `440`、回弹 `320`，缓动统一 `easeOutQuart`；**最关键的修复是删掉 `flip`**——旧实现在换盘过程中把主角从 15° 插值到接近侧身（中途扫过 60°+），参考的 `layoutPose` 里旋转**只取决于槽位**、与 `progress` 无关，所以磁带只平移后退、绝不逆着运动转身；这就是「很不自然」的来源。② **不再半透明**：相邻磁带改为**完全不透明 + 冷色压暗**（`.cas-f::after` 一层 `rgba(7,10,20,.7)→rgba(4,6,14,.85)` 的遮罩，`--cas-beneath` 只在非选中时为 1），原来那种会透出后方星空的半透明全删掉——参考里的邻书就是实心暗色物体。封面淡出也从参考的 `1−.96·side` **反转为 `0.8+0.2·(1−side)`**：不透明的前提下一路淡到看不见只会让后排变成空书脊。③ **构图对齐 `步骤4.png`（绝区零代理人秘闻选带界面）**：一屏 **9 盘**（`MAX_SLOTS 4`）、主角占画面宽 **28%**（参考约 1/3）、盘距压到自身宽的 **0.45**（`SPREAD_X 2.2 / MIN_WORLD_W 19`）形成密排叠压，后排 `Z_FALL 0.78→1.55` 深退拉开纵深、`RY_SIDE 1.46→1.01`（58°，参考值，90° 会把背面投成几乎零宽，那正是「侧面标题读不出来」的原因）。④ **垂直居中**：`.shelf-track` 的 `--shelf-drop` 13%→**1.5%**，实测主角中心与箭头中心差 **−7px**。⑤ **箭头闭合**：两根 4px 棒的顶点落在 `h/2 + len/√2`，所以容器宽必须正好是这个值——旧容器窄了 5px，两棒互相穿过，看起来是十字而不是箭头；现在用逐行像素剖面验证（两箭头都是对称 V、单一顶点、extent 只增减一次）。**两个箭头动画同相**且**同时向外再收回**（左向镜像所以关键帧符号取反；共用一套 keyframe 会让它们反向运动）。⑥ **修掉两个真 bug**：ⓐ `buildRing` 的环长不够——绘制带是 `current ± half`，所以环长必须 ≥ `n + 2·half`，否则带子撞到环尾、12 个 lab 条目只画出 6 盘；ⓑ **换语言丢卡片**：`buildRing` 会调 `updateUI`，而 `updateUI` 正是**写** `rrsuika-shelf-card` 的地方——原来在 `buildRing` **之后**才读它，读到的永远是刚被 boot 写进去的 "0"；改成模块作用域**只读一次**。⑦ **卡顿**：`will-change: transform` 从「每盘都有」改成只给选中盘（十个 3D 盒常驻独立合成层会拖慢并触发重栅格），转盘自转与封面淡出都加了「值变了才写」，静止时整行**一次写入都没有**（`rowBusy` 门控也冻结 idle 呼吸）。见 §5.2、§9 决策 23、§11）
> - 2026-09-18 v1.19（**主题切换改成「圆圈波」：去掉结束时的闪一下 + 允许连点**）：`src/scripts/theme-transition.ts` 与 `global.css §15` 重写，**不再使用 View Transitions**。① **闪一下的真因**：旧实现在 `finished` 里立刻摘掉 `html.theme-reveal`，而「两层快照都 `animation:none`」这条规则正是挂在那个 class 下的——摘掉它等于把快照上的默认交叉淡入淡出放回来跑一帧，正好和页面重绘撞在一起，于是看到闪。② **连点失效的真因**：View Transition **同一时刻只能有一个**，所以 `busy` 标志把揭示期间的每一次点击都丢了；用户要的「频繁点击产生黑白水波纹」在那个模型下不可能实现。改法：脚本往 `<body>` 追加一层 `position:fixed; z-index:90; pointer-events:none` 的覆盖层，**每次点击生成一个圆**——第一个圆直接长在覆盖层上（从按钮扩到最远角），之后的每个圆放进子容器；每个圆是 0×0、居中原点的盒子，增长完全靠 `clip-path: circle(0px at 0px 0px) → circle(R at 0px 0px)`，不碰 `transform`，原点因此永远精确。③ 多个圆同时跑需要**每个圆一个独立 keyframe 名**（`document.adoptedStyleSheets` 注入，这是脚本里唯一能加 `@keyframes` 的途径）——共用同一个动画名会让新圆把旧圆重启。④ 两条容易踩的坑：`apply(next)` 必须在圆开始生长**之前**执行（圆填充的是目标主题色，DOM 若还是旧主题，圆就会把「被替换掉的颜色」重新揭开）；覆盖层**每次点击都要重新填色**，否则第二次点击的圆会把上一次那个异色圆掀出来，看起来是浅色圆里冒出一个深色盘。实测：90ms 间隔连点 6 次 = 6 个圆同时在场、5/5 次都切换了主题；覆盖层淡出期间主题不再翻转（无闪烁）；单个圆的半径按 620ms 缓动从 424px 长到 1836px。**无覆盖层的路径**：reduced-motion 或还没有 `<body>` 时直接瞬切（实测覆盖层节点数 0），无坐标调用回落到按钮中心。见 §9 决策 23、§11）
> - 2026-09-18 v1.20（**修正 v1.19 的圆圈波：三个 bug 让动画退化成「闪一下」**）：v1.19 换掉 View Transitions 后用户实测「圆圈不见了，只剩闪一下，过一会恢复」。逐层排查出**三个独立缺陷**，缺一个都会退化成闪光：① **覆盖层自己被涂满了**——我给 `#theme-flood` 设了主题底色却**没有裁剪它**，于是它一开就把整屏盖成目标色，圆环动画虽然跑着但完全看不见，读起来就是「瞬间变色」。正解是**让覆盖层本身就是第一个圆**：它的 `clip-path` 从 `circle(0px at X Y)` 长到 `circle(R at X Y)`（X/Y 是按钮中心），圆外仍是旧主题、圆内是新主题；后续点击才往 `.theme-pulses` 里加兄弟圆。② **注入 keyframes 会重启所有动画**——我用 `document.adoptedStyleSheets` 给每个圆注入唯一 `@keyframes`（以为这是脚本里唯一的办法）。赋值 `adoptedStyleSheets` 会**触发全文档样式重算并重启所有正在运行的动画**，所以第二次点击把第一个圆打回半径 0，一轮连点下来什么都没画出来。正解是 **Web Animations API（`element.animate`）**，每个圆自带动画、互不干扰；唯一 keyframe 名根本不需要。③ **`(0,0)` 被当成合法点击点**——`element.click()`（以及任何合成 click）带的 `clientX/clientY` 都是 0，我原来直接采信，于是圆心跑到视口左上角，看起来像斜向擦除而不是从按钮扩散。现在 `(0,0)` 一律判为无效并回落到按钮中心（真实指针点击的坐标照常使用）。**验证方法**（值得复用）：截图有 8–20 帧延迟，620ms 的动画根本采不准；改为直接轮询 `getComputedStyle(layer).clipPath` 读圆的半径与圆心，实测 `0 → 1369px` 单调增长、圆心 `1062,37` 正是按钮中心。实测结果：7 次 80ms 连点 **6/6 都切换了主题**、同时有 7 个圆在跑；结束时覆盖层 opacity 归 0、`is-on` 摘掉、`body` 仍可点；reduced-motion 下覆盖层节点数为 0（瞬切）。见 §9 决策 23、§11）
> - 2026-09-18 v1.21（**箭头终于对了：两根棒必须「以中心定位 + 绕中心旋转」**）：用户反馈「两根线穿透了彼此的顶部，变成十字架」。**同一个 bug 我修了两次都没真正修好**，因为前两次都是靠推算而非看图。真相用**逐像素墨迹图**一测就出来了：两根棒在中间交叉，重叠处有约 9px 厚的「结」，然后再次分开——十字而非尖端。根因：两根棒的定位基准点不对。原来用 `left:0` + `transform-origin: 0 0`（**以棒的左端定位、绕左端旋转**），于是远端按旋转方向跑了整整 `L` 远，越过顶点继续伸出去。正解是**以棒的中心定位、绕中心旋转**：`left/top` 取对角线中点、`transform: translate(-50%,-50%) rotate(±45deg)`，此时斜线两端正好落在 `(0, h/2)` 与 `(w, 0)` 两个角上，只剩 `w` 一个自由量（`L = w·√2`、`h = 2w`）。实测墨迹图现在是标准人字：单顶点、`4 → 11 → 4` px 单调增厚再收细、两箭头分别指向左右。**顺带纠正两个我自己的错误结论**：① 之前写的「顶点在 `h/2 + len/√2`」是错的（那是「以左端定位」的产物，正是 bug 本身）；② 用 `getBoundingClientRect`/`getComputedStyle` 判断两棒是否交叉是**判不出来**的——父级 `scaleX(-1)` 不会出现在子元素的 computed transform 里，且矩形框无法说明重叠；扫描窗口开大了还会把主题按钮和计数条一起扫进去得出垃圾结论。只能**解码截图看墨迹图**。见 §5.2、§9 决策 23、§11）
> - 2026-09-18 v1.22（**箭头指向与尖角：折线的「尖」是中间那个点；两根棒永远做不出尖角**）：用户报两条——「两个箭头变成朝内了」「尖不是直角，是一个凹陷的平」。① **方向**：真相是**折线的尖端是它的中间坐标，不是第一个**。我按首坐标推理，把 `M2 2 L12 13 L2 24` 读成「起点在 x=2 所以朝左」，其实它画的是 `>`（顶点是 (12,13)），于是左右两条路径正好装反，一对箭头齐齐朝内。修法只是把两个 `d` 互换：prev = `M12 2 L2 13 L12 24`（顶点 (2,13)，朝左），next = `M2 2 L12 13 L2 24`（顶点 (12,13)，朝右）。② **尖角**：旧的两根圆头短棒**在任何摆位下都做不出尖角**——每根棒以圆帽收尾，两个圆帽正好在顶点处啃出一个凹口，那就是用户看到的「凹陷的平」。改成**一条描边折线**（`stroke-linejoin: miter` + `miterlimit: 8`），尖角由几何自己长出来；米特尖比 14px 的框向左/右多出约 1px，靠 `overflow: visible` 放出去。量出来的判据：修好后顶点那一列是 **2px、1 段**（一个点），两根棒版本是 **2 段**（缺口）。③ **一对关键帧不能用 `animation-direction: reverse` 省掉**：两条箭头是两条不同的路径，没有可共用的变换，`translate` 是**奇函数量**必须手写取反；`reverse` 是沿**时间**镜像，只有当关键帧关于 50% 反对称、且 `scale` 关于 50% 对称时才等价于空间取反——而对称的 scale 恰好会丢掉「回缩变细、外推变粗」的对比。定稿是「**translate 全部取反、scale 阶梯逐字照抄**」两套 keyframe（`shelfBounceOut` / `shelfBounceOutLeft`）。动画挂在内层 `svg` 上，`.shelf-arrow` 上什么都不挂，将来若再加镜像不会和动画抢同一个 transform。④ **两个测量技巧**（本轮靠它们定案）：墨迹图必须是**两次差分求交**（箭头→隐藏→箭头），因为星空在两拍之间会漂移，一颗移动的星只落在其中一次差分里，相交即可滤掉（顺手把 `canvas` 全隐藏更省事）；**方向可以直接从墨迹图读出来**——人字只有一列是「单段」（尖），另一头必是两段（两条尾巴），这个不对称**就是**方向，实测 prev 尖在左、next 尖在右，各是 2px/1 段。⑤ **同相向外**的实测：轮询 `getComputedStyle(svg).translate`（注意 `display:none` 的那一侧**根本不会动**，首屏 prev 是隐藏的，测之前要先 unhide），27/27 个采样点两箭头符号相反、prev 峰值 −8px、next 峰值 +8px 且同时到顶。见 §5.2、§9 决策 22b、§11）
> - 2026-09-18 v1.24（**磁带盒外壳：不透明 + 浅色主题改暖色 + 新拟物材质细节**）：用户三条：①「除了选中的都是半透明，light mode 下产生叠加的磁带体积」；②「light mode 的磁带盒改成偏暖色，不要黑色，太突兀」；③「参考新拟物风格加立体浮雕、四边金属质感银白螺丝、两个滚轮中间的方框和真实产品细节，可以参考 RhineLabUI 的卡片视觉（磨砂/亮塑料/金属）」。① **半透明的真因是 `--cas-front-o`**：v1.16 为了「侧身时封面别完全消失」把封面 opacity 在侧身时压到 0.8，于是每个邻盘的正面板都是半透的 ✗——浅色主题下背后那盘、再背后的壳、再背后的页面一层层透出来，就成了「叠加的磁带体积」。**该变量与它的 JS 写入一起删除**：磁带只用 `--cas-o`（= `tape.vis`，只在环进出与开场时 != 1）。实测 10 盘在场、所有面 `opacity` 均为 1、`--cas-front-o` 不再被写入 ✓。深度改由**不透明的压暗纱层**（`.cas-f::after`，`--cas-beneath` 门控）+ Z + scale 承担。② **外壳改成「材质 token」**：`.cas` 上一套 `--shell-top/mid/bot`、`--shell-spine-a…d`、`--shell-edge-*`、`--shell-back`、`--shell-rim/inner/hi/ao/seam`、`--relief-hi/lo`（全盘统一左上光源）、`--metal-*`、`--shell-ink*`、`--glass-*`、`--shell-dim-*`、`--tape-drop-*`、`--key-glow`；浅色主题整组换成**暖骨白/象牙**+暖阴影（连脊背文字也要翻成暖深色，否则米白底上白字消失）。金属两主题都是金属（浅色是暖银：实测螺丝 `rgb(145,134,116)`，深色 `rgb(104,104,108)`），**只有观察窗腔体两主题都保持深色**（那是腔体不是壳）。③ **新拟物硬件**：四角螺丝（`.cas-screw` 径向镍面 + 一字槽）、J-card 压制唇边（`.cas-label::after` 左上亮右下暗的硬边内阴影）、塑料高光扫过（`.cas-label::before`）、凹陷磨砂观察窗 + 两个带金属轮毂的转盘、转盘之间的**凸起铭牌**（`.cas-plate`：磁带类型 / 长度 / 印刷条码）、窗下的**走带路径与两个导带轮**（`.cas-path`）、壳身浮雕型号（`.cas-mould`：透明字 + 亮/暗双层 text-shadow）、脊背金属压顶与模压刻线、上下壁的模压凹槽、背板的模压棱纹与凹陷标签位。④ ⚠️ **性能与「量之前先确认样式表真的解析了」**：外壳细节住在全部 10 盘上、换盘时每盘每帧都会重绘，所以一律用**硬边 + 径向渐变**（唇边只剩 3 条硬内阴影、霜层并进窗自身背景、螺丝 2 层阴影、转盘硬环替代内模糊、背板去掉一层重复渐变、邻盘投影从 64px 模糊换成 12px 短投影）。**有效**的换盘实测：headless 基线本身就 ~80ms/帧，带上细节 80.5–93.5ms（首次含冷栅格化 +6.6ms，热起来 0–2ms）——**能负担，但不免费**。⚠️⚠️ **本轮最重要的教训**：我编辑 `.cas-label::after` 时留下了一个多余的 `{`，于是 `<style>` 里**它之后的所有规则都被浏览器静默丢弃**——`astro check` 通过、`npm run build` 也通过、页面照常渲染，只是外壳细节全没了。我基于那份坏样式表量出的「28fps → 21fps / +17ms」全是假的（那时细节根本没生效）。**改完这个文件的 `<style>` 后，必须先从 `document.styleSheets` 里确认新选择器在不在，再相信任何布局/颜色/帧率数字。**⑤ **浅色主题的压暗要更强**（`--shell-dim-*` 0.46/0.58 vs 深色 0.68/0.84）：邻盘封面本来就是亮图，实测浅色下纱层把封面压掉 **43** 亮度（深色下只有 2，因为深色封面本来就暗）。象牙底上的纵深要用暖褐纱换，不能用冷纱。见 §5.2、§9 决策 22b、§11）
> - 2026-09-19 v1.25（**去掉螺丝 + 中间框重做 + 主题切换把耗时挪出点击 + 站点开场预加载**）：用户四条反馈。① **「螺丝太丑了，去掉他们」**——四角螺丝（40 个 CSS 画的小圆）在 26px 尺寸下读起来是「一个浅色点」，不是五金件，整排因此显得像玩具；markup、样式、`--metal-edge/slot` token 一并删除，`--metal-*` 只留给转盘轮毂与脊背压顶。② **「中间框很丑，看起来很廉价」**——真因是**小字**：第一版在 ~65px 高的窗口里塞了三行 5px 微字 + 假条码，那是印刷噪点而不是细节（也是 `design-taste-frontend` 那条「诚实材质」原则的反面教材）。改成：一个内凹槽 + **一行** 0.46em/0.22em 字距的技术标（`TYPE II · C-90`）+ 一枚 2px 强调色短杠（窗内唯一的颜色），假条码整段删除。③ **「切换依旧有点卡顿」**——实测定位：`cloneNode`+插入本身只要 1.4+1ms，但浏览器随后要为 ~700 个节点 layout+paint，**那一帧 101ms**，正好落在点击瞬间（这就是「卡顿」的全部来源；reveal 本身的持续开销在 headless 里主要是软件光栅）。修法两层：**`pointerdown` 预热**（`buildWarm` 把下一击需要的拷贝用 `visibility:hidden` 先排版好，`claimWarm` 在点击时取用，且只在 `WARM_MS` 400ms 内有效 → 永远是「此刻页面」的快照；键盘激活没有 pointer 事件，照旧在点击时构建），**以及圆的 tween 先 pause、下一帧再 play**（新层最贵的一帧发生在半径 0，那里什么都看不见，于是卡顿被挪到看不见的地方）。实测：点击同步耗时 100ms → **7.5ms**，预热拷贝在点击前确实存在。④ **开场动画（Rhine Lab 风）**：`Layout.astro` 的 **SITE INTRO GUARD**（内联 head 脚本，在首帧之前决定）+ `src/scripts/site-intro.ts`（视觉）+ `global.css §16`。两半张纸 + 发丝接缝 + 四角套准十字 + 进度轨 + 百分比，页面能真正画出来时（黑洞视频有帧、字体就绪、`window.load`、再两帧 rAF）像文件一样从中间打开。**判定必须放在 head**：模块脚本在 body 解析后才跑，晚到足以让「站内跳转」先闪一下封面再撤掉——用户要的三态是**会话首个页面放、F5/重载放、站内跳转不放**（`sessionStorage["rrsuika-intro-seen"]` + `performance.getEntriesByType("navigation")[0].type`；本站是纯多页构建、没有客户端路由，除此之外没有别的办法区分「首次访问」和「点了链接」）；`back_forward` 与 reduced-motion 一律不放。它同时也是**预加载闸门**：等视频/星空/字体/图片就绪才揭幕。⚠️ 绝不能让开场把站点锁死：`MIN_MS` 900（不至于闪一下）、`MAX_MS` 4200、guard 自带 6s 兜底（连 `__rrsIntroGate` 一起 resolve）、`<noscript>` 直接隐藏、skip 路径在 `DOMContentLoaded` 把节点删掉（不然每个页面都带着一个隐藏的整屏 div）。guard 通过 `window.__rrsIntroGate/__rrsIntroResolve` 把「揭幕」这个时刻交给书架：书架的幕布与升架现在等它，两段入场于是**顺序播放**而不是叠在一起。实测 7/7：首访显示并在揭幕后自我移除、F5 重放、站内跳转不放、reduced-motion 不放、螺丝 0 个、面板每盘一个标、点击耗时 7.5ms。见 §5.1、§5.2、§9 决策 23、§11）
> - 2026-09-19 v1.26（**开场跟主题走 + 轮毂从凸起改凹陷**）：用户两条。① **「假设是 dark mode，开场动画是暗色；目前在 dark、light mode 下开场都是亮色」**——v1.25 我把开场做成「两主题都是白纸」（照搬 Rhine Lab 的原片），在这站上读起来是闪光弹 ✗：开场是页面的第一帧，所以必须是配色的第一帧，而 boot 脚本本来就在首帧之前设好 `data-theme`，所以完全能做到。现在 `.site-intro` 的 paper/ink/rail/dim 是 token，`:root[data-theme="dark"] .site-intro` 换成 `#0a0a11` 底 + 米白字，浅色仍是 `#f1ece1` 底 + 深字。实测：dark 渲染角点 `rgb(10,10,17)`（lum 11）、light `rgb(241,236,225)`（lum 236）✓。② **「轮子从凸起改成凹陷，弱化质感，让他更高级」**——原先 `.cas-reel`/`.cas-reel-hub` 的高光在左上（34%/26% 的径向 + 亮环），那是**凸起**的打光；凹陷的正确打光是反的：**近侧壁（左上）落影、远侧壁（右下）受光**。现在两处都用 `inset 0.5px 0.5px 1px rgba(0,0,0,.8)`（近壁影）+ `inset -0.5px -0.5px 0 rgba(226,218,202,.16)`（远壁光）+ 一根发丝切边，径向渐变的光心挪到右下，同时把对比度整体压低（辐条 0.62→0.34、环 0.34→0.16）——「弱化质感」在这里的具体含义就是**别用明亮的整圈高光**，那会让它读成一个按钮。实测（屏幕上下轴，15° 侧转对它几乎无影响）：深色主题轮毂上 25 / 下 54，浅色主题 29 / 59 ✓；井底同样上暗下亮 ✓。见 §5.2、§9 决策 22b、§11）
> - 2026-09-19 v1.27（**浅色外壳换冷铬银 + REELS 面板修好 + 选中盘专属 pitch + 圆环只在选中盘转 + 开场机能化 + 烘焙 HERO 参数**）：① **浅色磁带盒改冷铬银**：上一版是「暖象牙壳 + 暖棕阴影 + 暖纸背景」，三者同色温、没有对比，用户判为「像屎黄色」——正确。复古未来主义在这张暖纸上应该是**金属压纸**：冷银壳、蓝灰阴影、冷炭墨字，**整盘唯一的暖色留给强调色条**；实测渲染侧脊 `rgb(140,144,152)`（R−B −12）。② **REELS 面板没反应的根因**：`--reel-*` 默认值原本写在 `.cas` 上，而面板把覆写写成 `<html>` 的 inline 属性 —— **`.cas` 自己声明的自定义属性会完全遮蔽继承值**，于是面板写什么都被无视（HERO 走 `window.__shelfTune` 所以正常）。修法：默认值搬到 `:global(:root)`，覆写才赢得进来。⚠️ 通用教训：**可被外部覆写的自定义属性，默认值必须放在作用域的根，不能放在被覆写元素自己身上。**③ **新增选中盘专属 pitch**：用户要「能调看到他的底部或顶部的坐标」，而 `RX_BASE` 是**整行**俯仰（一调十盘一起动 ✗，当场被发现）。新增 `RX_HERO`，用与 `LIFT` 相同的 `heroMask = exp(-p²·LIFT_FALL)` 衰减 → 只影响选中盘 ✓（实测 hero rotateX 变化、邻盘 0.075 → 0.075 不变）。④ **圆环只在选中盘转**：原条件是 `hero.abs < 1.7 && rowBusy`，于是静止时整个架子完全不动、只有拖动时才活，被报为「圆环只在拖动状态下才会动」。现在只给 `hero.abs < 0.5` 的选中盘写 `--reel-spin`，静止时 0.22 rad/s 慢转、换盘时加速；其余九盘固定角度（也是最省的状态：一盘重绘，九盘不重绘）。⑤ **开场机能化**：去掉四角套准十字（那个尺寸下像印刷笑话），轨道从 1px/260px 放大到 2px/460px、加发光填充 + 一枚随进度移动的亮头，百分比放大到 20px，另加一条向下扫过的扫描带（Endfield 那种「整体静止 + 一件东西扫过」的机能感）。⑥ **去掉正面斜高光**：一片横跨封面图的镜面高光读起来是「贴在画上的贴纸」而不是塑料反光；要加高光就加在**壳**上（边或唇），绝不横跨印刷面。⑦ **左上角对齐**：`.shelf-head` 原本是 `max-width: var(--container-wide); margin: 0 auto`，在 1600px 下把 eyebrow 推到 x=140，而 HUD 的 `ARCHIVE FILE` 在 x=34 —— 两个左边缘明显不一致。header 改成与 HUD/面板/页脚同一个 34px 边距、不再居中（实测两边都是 34 ✓）。⑧ **烘焙用户给的 HERO 参数**：`RY_CENTER 0.222`、`RX_BASE -0.13`、`RZ_BASE 0.074`、`LIFT 0.21`、`Y_BASE -0.18`、`Z_NEAR 2.6`（同时同步到面板默认值，使 RESET 回到发版值）。探针 5/5。见 §5.2、§9 决策 22b、§11）
> - 2026-09-19 v1.29（**拖动释放：一个手势最多一格；向左拖修好；lab 末端的左箭头修好；空白磁带不可选中**）：用户报三条。① **「从左往右拖动时，松开鼠标他会回到之前选中的磁带盒」**——落点区间被写成了 `clamp(round(progress), −maxStep, 0)`。看起来无害（前进就是负 `progress`），但它让**每一个向右拖都变成空操作**：300px 的右拖到达 `progress = +1.07`，取整得 `+1`，然后被这个区间夹回 `0`，于是整行原路弹回原来的磁带。区间改成对称的 `[−maxStep, +maxStep]`，只有甩动（`FLICK_COMMIT`）还能加一格。② **「在 lab 拖动到最右侧（ABS 3D Printing）时按左箭头无法切换」**——两个独立缺陷叠在一起：ⓐ `recycle` 的 re-base 区间用的是**环的两端**（`[1, list.length−1]`）而不是**可走区间**，而 `/lab` 的 `walkTo` 恰好落在环端之外，于是 `next = walkTo` 被判越界、步骤被拒，`settleDrag` 已经写进 `spring.target` 的落点却留了下来——十几秒后弹簧落地，把选择**反向**挪了一格（挪到空白上）；ⓑ 更直接的一个：落点是从 `current − dragOriginCurrent` **在落定时反推**的，而 `current` 会被 `recycle` 在弹簧飞行期间 re-base，两个快照一起过时就得到 `step = −16`，`wrap` 把它读成「往另一边十六格」，整行从最后一盘瞬移到第六盘。现在落点是**方向**（`spring.dir`，−1 = 前进）在落定时施加到**实时** `current` 上，并且在落定的同一刻**被消费掉**（`spring.dir = 0`）；不消费的话弹簧和任何修正会各自反复重放同一个方向，`current` 在最后一盘和它外面的空白之间无限振荡（实测 `{16→17, 17→17, …}`，`lock` 永不释放）。③ **「拖到空磁带盒时缺乏过渡」**——用户描述的理想是「先回退到第一个空磁带盒再到有内容磁带盒」。我用四种写法试过，全部失败，**最后决定不这么做并记下原因**：把空白当作合法落点，弹簧就必须先把整行**推进去一整格**（那是它自己的行程），修正就变成方向相反的**第二个整格**——读者看到的是「被甩出去再被拽回来」，比没有过渡更糟；而 `recycle` 也无法表达它（一个手势里 re-base 两次，正是它的区间检查存在的意义）。现在改由 `spring.target` 给出**一格**的让位（不是 `DRAG_MAX`：对准 1.6 格时弹簧会凭惯性冲到 `progress = −2.82`，也就是比预期深 1.2 格，而且过冲只在**落定后**才被修正，于是看到的是「冲出去一整格再弹回」），落点始终夹到真磁带上：实测 `progress` −1.6 → 约 −2.2 → 平滑回 0，末端读起来像一堵墙而不是死区。④ ⚠️ **随手记下的两条通用约束**：`recycle` 的 "落在当前格" 分支必须看 `step` 而不是 `|progress|`（缓慢松手时弹簧停在整数目标上、`progress` 仍是千分之几，该走的一步会被丢掉）；释放弹簧必须有**硬性时长上限**（`SPRING_MAX_MS 620`），只靠速度阈值不封顶时长，滑行结束后 `lock` 一直不释放，之后每个手势都被拒。⑤ 实测（CDP、1700px、`SLOT_DRAG` 280px，两个列表页都测）：拖 200px 以上＝正好换一格、150px 以下＝不动、快慢不影响距离；两端越界拖动＝选择原地不动；两端箭头各走一格。见 §5.2、§11）
> - 2026-09-19 v1.28（**拖拽重做成连续行程 + 惯性滑行；烘焙 REELS；哑光轮盘/磨砂铭牌；浅色换暖白亮塑料；空磁带做设计；开场扫描带与进度条照终末地重做**）：用户六条，全部落地。① **拖动（本轮最大的重写）**：旧手势是**滑动开关**——只量 `pointerdown → now` 的位移，`progress` 夹在 ±1.15，过 `max(92px, 6.5vw)` 就固定换一盘、否则回弹，于是「300px、600px、3000px 拖出来一模一样」，用户原话「鼠标目前拖动只能只能拖动一个磁带的切换，我希望慢慢拖能慢慢拖动磁带，而快速的拖能快速切换多个磁带，同时松开后根据拖动的力度加一个缓存渐出的体验」。现在：`dragOffset` 累计**整个手势的行程**，`SLOT_DRAG()`（`clamp(170, 视口×0.17, 280)` px）买一格，所以一格以外的距离真的会一格一格地走；松手时**位置决定落点**（过中线就进位）、**速度只投一票**（`|v| ≥ FLICK_COMMIT 2.6 格/秒` 且方向一致时多走一格），并把释放速度交给**临界阻尼滑行**（`GLIDE_RATE 5.4`，比回弹用的 9.2 慢，`1/rate` 就是衰减时间常数，110ms 读起来像被拽走）。⚠️ **「位置 + 投掷位移」这个显然的物理答案实测不成立**：`max(Δ)=v0/(e·rate)` 在约 7 格/秒时就等于一整格，调弱到没用、照实算则普通快甩会冲过两盘；而且两个效应在**取整边界**上互相打架，一次硬甩会落到指针**后面**。② **四个真 bug（都会让拖动「没反应」）**：ⓐ `settleDrag` 的落点区间写成 `clamp(round(raw), walkFrom-current, 0)`——**在第一盘上这个差是 0**，于是从档案开头起怎么甩都落回原地；ⓑ `spring.land` 是**相对格位偏移**却被当成**环索引**用（`-1` 读成「第 -1 环」→ step = `-1-4 = -5` → `recycle` 拒绝 → 把弹簧重新拉到 `progress=0` → 下一帧再来一遍，**死循环**，盘子永不移动）；ⓒ `recycle` 用 `|progress| < 0.5` 判断「落在当前格」，而缓慢松手时弹簧停在整数目标上、`progress` 仍是千分之几 → 该走的一步被丢掉；ⓓ 弹簧只在**超过 `SPRING_MAX_MS 620`** 才强制判定落定（否则速度阈值不封顶时长，滑行结束后 `lock` 一直不释放，之后每个手势都被拒——表现为「甩一下之后卡住」）。另外：`dragSamples` 必须**每个手势清空**（残留采样会把两次拖动之间的空隙算成一个极快的 slope，方向还是反的），释放速度改成**窗口内最小二乘斜率**（两点差分在报告率一低就只剩噪声）。③ **环的余量**：`recycle` 只能落到「还能把绘制带画出来」的格位上，而 `2·half` 的补白刚好把环填满、一格不剩 —— 于是**任何**快甩在中段也被拒。现在补白 `2·span`、`MAX_FILLERS 4×MAX_SLOT_COUNT`（16 张空壳，`/projects` 环长 15、`/lab` 22），并且 `settleDrag` 那一行不再看 `walkTo`（落点是格位偏移，正负号与环索引相反）。实测扫描（`/projects`，1400→900 起点）：慢拖 60px → 0 盘、160px → 1、300px（整格）→ 1、430px → 2、600px → 2；快甩 200–700px → 2 盘；两端的甩动 → 0（档案尽头本来就到头了）。④ **烘焙 REELS**：用户给的一整套值写进 `:global(:root)`（`--reel-size 3.1em`、`--reel-hub-inset 34%`、`--reel-spoke-*`、`--reel-near-shadow .81`、`--reel-far-light .4`、`--reel-rim .21`、`--reel-cavity #050505`、`--reel-occl rgba(0,0,0,.44)`），并**删掉 `hub light X/Y` 与 `hub lit` 两个滑杆**（连同 `--hub-lit-*`/`--hub-shadow` 的接线）——⚠️ **轮盘和它的框从此是哑光的，用光照而不是用亮环做凹陷**：三档高光渐变整体删除（那正是「明显的高光渐变，会让质感很廉价」），井底是**平色**，凹陷只由「近壁一道暗、远壁一道亮」两条发丝线加 `::after` 里一条 `radial-gradient` 遮挡带承担。⑤ **去掉环下的小球**：`.cas-path-roller` 两个 0.42em 的球整个删掉（markup + 样式），走带路径只留一条丝带。⑥ **铭牌改磨砂塑料**：`.cas-plate` 原本是近黑填充 + 底边一条亮线 + 会发光的强调色短杠 —— 黑底加镜面线就是铬。现在填充是**奶白半透明**（浅色 `rgba(255,250,234,.3)`、深色 `rgba(176,172,164,.24)`），配 `::before` 一层很宽的竖向渐隐，没有一处亮边；浅色主题的墨色反而**要浅**（`--plate-ink` 白 86%），因为填充压在深色腔体上之后是中间调，深墨会糊掉。⑦ **浅色外壳改暖白亮塑料**：用户否掉了冷铬银（「那个时代更多是一种偏黄偏白的塑料，带高光感的」）。这一版和 v1.27 的教训是**一对**——v1.27 说了「同色温会糊成屎黄」，所以这次不是把整体调暖，而是**暖壳体 + 冷阴影 + 真镜面高光**：`--shell-top #f2e9d5 / mid #e0d5bb / bot #bfb094`（偏黄偏白），阴影与纱层**保持冷蓝灰**（`rgba(34,44,62,…)`、`rgba(42,50,66,.4)`），高光落在 `.cas-label::before` 的**左上角径向衰减**（`--gloss-a .4`、`--gloss-c #fff7e0`，浅色 0.4 / 深色 0.1）。⚠️ 高光必须是**角落衰减**而不是横跨画面的斜带——斜带正是 v1.27 删掉的那条「贴纸」。⑧ **空磁带做设计**：以前是一块深色网格 + 一个发丝十字，读起来是「空槽 + 故障」。现在是一张**没写过的 J-card**：`{brand}` + `TYPE I` 的印刷标题带（17.5% 高）、`LOW NOISE · C-60` 的型号、`--blank-sheet` 一块浅色纸兜（33%–75%）、四条可书写横线（`background-size: 100% 42%` + `background-position: 0 33%`）、软阴影。**去掉强调色条、去掉书脊色条**（空白磁带是架子上最安静的东西），轮盘的辐条也隐藏（那是一整卷带）。⑨ **开场照终末地重做**：用户的判据是「扫描带和进度条太丑了」——旧扫描带是一条 24vh 的琥珀渐变**向下**飘两次，进度条是 2px 半透明轨 + 实心强调色填充 + 一枚发光亮头 + 填充下的光晕，**光晕就是廉价感的来源**（2px 线上挂一圈模糊读起来是一根灯管）。参考站的设计语言是**印刷 + 仪器**（奶油纸、墨、全直角、一个饱和信号色），所以：扫描带改成 **2px 竖线从左到右扫一次、`linear` 缓动**（扫描仪不加速，线性正是它读起来像机械的原因）；进度条改成**带刻度的标尺**（1px 轨 + 每 5% 一道细刻度 + 每 10% 一道重刻度，填充是一块纯强调色方块盖着刻度往上爬，亮头是一根 3px 的强调色横杆，无模糊）；百分比从 20px 放大到 **34px 并删掉 `%` 号**（改成小号 `% LOADED` 单位），开启 `tnum` 等宽数字（计数器每帧重绘，比例数字会让整条读数在 11→22→33 时抖动）。⚠️ 原尺寸的刻度在 1px 轨上是**不可见**的，所以刻度用 `--intro-rail/-on` 两个 token，它们是 `color-mix(accent n%, paper)` —— **在两种主题下各自混到底色**，这是同一组 alpha 做不到的（强调色在纸上是很深的橙、在黑上是很亮的琥珀）。⑩ **仍然没有视觉验证**：本环境没有视觉通道，所有材质结论都来自 computed value 与 DOM 测量（`--shell-*`/`--glass-*`/`--plate-*`/`--blank-*` 全部逐条读出、hero 的两个井都在窗口内、井间距 320.8px、`--reel-spin` 只有 1 盘在写、`will-change` 只有 1 盘、静止时 transform 冻结、`/projects` 画 11 盘、重建 88 页通过）。**配色和高光的「好不好看」我判断不了**，需要用户看。见 §5.1、§5.2、§9 决策 22b、§11）
> - 2026-09-19 v1.32（**拖动落点方向修正（真 bug）+ 开场量表改双条对开 + 去掉百分比荧光 + 浅色外壳提亮 + 可调 hub 尺寸/质感/透明度**）：用户四条。① **「拖动方向逻辑是反的：我从左往右拖，比如从 5 拖到 4，松开鼠标后会变成拖到 6；从右往左也是如此」**——**两个符号错误叠在一起**，都在 `settleDrag` 里。`progress` 随指针向右**增大**（`dragOffset = clientX − dragAnchor`，行跟着手走，这一点本来就对），而 `slotIndex = ring − current + progress`，所以拖着行停在 `progress = p` 时**正中那盘是环索引 `current − p`**。落点却写成 `spring.dest = clamp(current + land)`：既选中了指针**背后**那盘，又因为 `recycle` 只有在实参等于 `−progress` 时才保持画面不变、而落点已经把 `progress` 留在 `+land`，一次 re-base 把整行推了 `2·land` 格。实测（`/lab`、1700px、一格 280px）：改前「从 5 向右拖一格」→ 5 直接变 **6**（画面横跳两格），正是用户报的现象。现在 `dest = clamp(current − land, walkFrom, walkTo)`，`recycleFromSpring` 的余量同步改成 `over = round(spring.target) + step`（recycle 需要的是 `−step`，之前是减号，只在旧的错误约定下才自洽）。改后实测：**右拖 1/2/3 格 → −1/−2/−3，左拖 1/2/3 格 → +1/+2/+3**，不足半格 → 0，两端越界 → 停在档案尽头；`/lab` 起点为第 1 盘时向右拖恒为 0（那是档案近端，本来就到头）。② **同一个函数里还有一个「多跳一盘」的隐患**：甩动加票原本是 `land ±= 1`，**不看取整是否已经走过那一格**——0.8 格的快甩会 `round→1` 再加 1 = 2，跳过一个拖拽从未到达的磁带。现在改成「只把取整结果补到**运动方向上的下一个整格**」：`land = max(land, ceil(progress))` / `min(land, floor(progress))`。实测（临时探针读出内部量）：速度 4.0 格/秒时 `progress 1.143 → target 2`（补到 `ceil`）✓；速度 1.9 格/秒时不加票 ✓（阈值 2.6 正常工作）。⚠️ 顺带记下**探针经验**：Chrome 会把 `Input.dispatchMouseEvent` 连发的 move **合并**成 2–3 个 pointermove，所以合成指针的实测速度远低于派发速度（90px「快甩」只量到 1.9 格/秒），**短距离甩动的合成测试天然不可信**，要用探针读 `spring.velocity` 才能分辨「没触发」和「触发了但落点一样」。③ **开场量表改两条、从底部左右两端往中间合**（「改成两条，分别从画面底部的左右侧往底部中间移动」）：原来是一条满宽、从右往左填。现在左边一条向右长、右边一条向左长，100% 时在正中相遇。⚠️ **两条 live 层都是满宽、各自被 `clip-path: inset()` 裁成半边窗口**（`inset(0 calc(100% − var(--reveal)/2) 0 0)` 与镜像）：若真做成两个半宽元素，**拼接处的条纹相位会错开**——中心位置在几乎任何视口宽度下都不是 11px 周期的整数倍（1700px 中心 = 850，850 mod 11 = 3），两半的 `repeating-linear-gradient` 会各从自己的左边缘起算。满宽同一条渐变的两个半窗天然同相位，且半窗不可能重叠，连层级都不用管。实测：`--reveal 20.99%` → 两侧各 `inset(... 89.505% ...)`（= 100 − 10.495）；`92.65%` → `53.675%`；亮条纹 `rgb(232,148,58)`、暗条纹 `color(srgb .266 .180 .108)`（= signal 26% 混纸）✓。④ **去掉百分比的荧光**（`text-shadow` 两条全删，`transition` 只留 color），实测 `textShadow: "none"` ✓。⑤ **浅色外壳太暗、侧面标题看不清**——两个原因同时存在：外壳本体比它站着的纸面暗一整档，邻盘纱层又拿走约 45% 的光。实测（同一批 5 个邻盘，取标题矩形内亮度的 90/3 分位算 WCAG 对比）：**改前最差 2.6:1（低于大字 AA 的 3:1）、均值 5.02:1；改后最差 4.4:1、均值 6.25:1**。拆开看：只换外壳 6.13:1、只换纱层 5.18:1，所以**两者都必要**。token 整体提亮一档（`--shell-spine-b #c4b89c → #d5caae` 等）、`--shell-ink` 加深（`rgba(52,42,24,.88) → rgba(38,29,14,.93)`）、`--shell-dim-a/b` 从 `0.40/0.54` 收到 **`0.26/0.38`**（仍保持冷色）。⚠️ 教训：**浅色主题里深色墨字标在深色纱层上＝自己打自己**，纱层的浓淡其实是标题对比度的第一决定项。⑥ **调参面板新增 hub 尺寸/质感/透明度**（「给我添加能移动 hub 的长度和宽度的变量，能调整 hub 的质感和透明度的变量」）：`--reel-hub-inset` 删除（一个 inset 只能描述圆），改成 `--reel-hub-w` / `--reel-hub-h`（默认 32%/32%，即原来 `inset: 34%` 画出的同一个圆盘；两者不等则成椭圆），外加 `--reel-hub-alpha`（孔面透明度，<1 时辐条会从孔面透出来）与 `--reel-hub-gloss` / `--reel-hub-grain`（孔面径向衰减深度、细密车削环纹），后两者**出厂为 0**——出厂为 0 的旋钮不可能悄悄把用户否掉的高光斜坡带回来。孔面画在 `.cas-reel-hub::before`，透明度用 `calc()` 写进 alpha，一个滑杆就能缩放整层。实测：`--reel-hub-w/h 44%/20%` → hub 盒 24×24 → **32×16**，`alpha .97 → .4`，gloss/grain 生效，复位后回到 `rgba(7,7,10,.97)` ✓。⑦ **扫描色板改成纯色块**（「我希望顶部没有白色渐变，是一个纯橙色块的」）：删掉 `.site-intro__sweep::after` 的顶部亮唇与 `--intro-sweep-hi` token，色板现在是一条 `background: var(--intro-signal)` 的实色——上行的边缘是硬边，本身就是画面里对比最强的东西，不需要再给一道高光。⑧ 页脚版本号 `2026.V8.16 → 2026.V9.19`（大版本重构手动升版）。见 §5.2、§11）
> - 2026-09-19 v1.31（**开场照第二版 `输入/开场动画.html` 重做：计数器真的跑 0→100 + 退场改成信号色扫描 + 底部单条量表**）：用户两条——「我希望开场动画至少有快速是 0-100 的切换，另外参考我提供的新的开场动画.html」（该文件当天 09:08 被用户替换，26KB，上一轮那版已经不是它了）。① **计数必须跑到 100，而且快**：上一版是 `easeOutCubic` 爬到 85% 后用 `85 + (1 − e^(−t/2400)) × 14.95` 无限逼近 **99.95%，永不到 100**——那是「接近百分百时慢一点等加载」的诚实答案，但数字永远不显示 100，而渐近线的最后几个百分点和一帧冻住没有区别。现在照参考原曲线 `max(t, easeOutCubic(t) · 0.85)`（`COUNT_MS 2200`）：缓动的头 + 线性的尾，`t ≥ 1` 时**强制 1**；实测（1700px、热缓存）49% @0.55s、74% @1.1s、100% @2.2s。② **页面提前就绪 ⇒ 余量「快进」而不是跳变**（`SETTLE_MS 260`，且落地不早于 `MIN_MS 1400`，让入场编排演完）——实测热缓存 100% 落在 **1.43s**，这正是用户要的「快速 0-100 的切换」；⚠️ 一帧从 40% 跳到 100% 是计数器唯一绝对不能有的形状，所以余量是**跑完**的，不是赋值的。③ **计数走完而页面没就绪 ⇒ 停在 100**，由状态行说「Waiting on assets」（读数带脉冲）——等待由**文字**汇报，不靠假装数字还有地方可去。实测（用 `Page.addScriptToEvaluateOnNewDocument` 把 `readyState` 钉在 loading 并吞掉 `load` 事件）：100% @2.4s → 「Waiting on assets」→ 3.4s 到 `MAX_MS` 上限 → Ready + 开始扫描 → 封面 5.2s 消失。④ **状态阶梯由计数驱动，不由加载驱动**：按加载驱动时，快页面会从「Initialising」直接跳到「Ready」，整条 boot sequence 根本读不出来；现在 `Initialising → Loading assets(28%) → Calibrating layers(62%) → Ready(100%)`，只有「就差一点」和「好了」两个状态由真实加载决定。⑤ **退场从「两片幕布像文件一样分开」改成「信号色扫描」**（参考这一版自己换的）：满屏 `--accent` 色板从底边长上来（`scaleY(0→1)`，620ms，`cubic-bezier(.85,0,.15,1)`）→ 停 280ms（期间字标/读数/四角/量表按 0/70/100/140/180ms 依次淡出）→ `opacity→0` **加 `blur(7px)`** 720ms 消散。⚠️ **色板满高那一帧必须把底板、舞台、量表一起隐藏**（`.is-revealing`）：`.site-intro` 自己不带背景（背景在 `.site-intro__plate` 上）就是为了这个——不隐藏的话读者看着色板淡成**纸张**，再下一帧纸张被整块切掉。实测把色板钉在 `scaleY(1)` 后拍帧：`rect=[0,0,1700,900]`、`z-index:20`、两个主题各采样 18900 点，**0 个点落在色板色系之外**（深色 `rgb(232,148,58)`、浅色 `rgb(192,112,32)`，顶边是更亮的 `--intro-sweep-hi` 唇边）。⚠️ 参考里的 `drop-shadow` 没搬：它被 `.rail` 自己的 `overflow:hidden` 裁掉了，本来就没有效果，却要每帧重绘整条。⑥ **两片竖向量表改成一条底部满宽量表**（参考的几何）：`--reveal` 驱动 `clip-path: inset(0 0 0 calc(100% - var(--reveal)))`，即**从右往左**填充；实测条纹 8px / 间隙 3px 与原值逐字一致，暗条纹 `rgb(68,46,28)` 正是 `color-mix(signal 26%, paper)`，亮条纹 `rgb(232,148,58)`，填充边界随 `--reveal` 移动。⑦ ⚠️ **本轮最有价值的 CSS 教训**：入场动画**只能用 `animation-fill-mode: backwards`，绝不能用 `both`/`forwards`**。它们动的是 `opacity`，而**填满的末帧优先级高于一切普通声明**——用 `both` 时 `.is-sweeping .site-intro__mark { opacity: 0 }` 这类淡出规则会**静默失效**，动画一直把 `opacity: 1` 交回来。参考 html 用的正是 `forwards`，所以它那套「舞台内容依次淡出」在浏览器里其实什么都没做。⑧ 其余：四角标签改为**先到**（160/260/360ms，与参考的 200/350/500 同序）；状态文字切换加「降透明度 → 换字 → 回来」的微过渡（`SWITCH_MS 150`）；计数器放大且带 signal 辉光，ready 时辉光更亮；底板加一层带径向遮罩的机能网格（保持无前缀 `mask-image`，不写 `-webkit-` 别名对）。**仍然刻意不搬**：扫描线与色差文字阴影（复古终端那一半；这一版参考自己也已经把扫描线去掉了），信号色一律取 `--accent`。实测四条路径：热缓存 100% @1.43s、封面 3.34s 消失；首次导航（冷）679ms 时已 23%、曲线一致；页面永不 `load` 时 100% @2.4s 并停在「Waiting on assets」，到上限后 5.2s 兜底消失；两个主题的 `--reveal`、量表几何、四角与读数位置、色板覆盖逐项核对。见 §5.1、§11）
> - 2026-09-19 v1.30（**拖动「落点即行程」+ 空白落点滑回磁带 + 星空渐入 + 开场照 `输入/开场动画.html` 重做**）：用户四条。① **「水平移动拖到一定距离就无法继续拖动，而这时才拖动了 2 个磁带盒的距离…无论我滑动多块，比如从 1 滑动到 5，松开鼠标后又会回退到 2 而不是 5」**——v1.28 的滑行仍然把**幅度**丢掉了一半：`DRAG_MAX` 只有 1.6 格，手势再长也被这一条夹住（第 2 盘就是墙）。现在 `DRAG_MAX 1.6 → 5`：`dragOffset` 是**整个手势的行程**，`SLOT_DRAG()`（`clamp(170px, 17vw, 280px)`）买一格，`progress` 就是格数偏移（手势期间**不设上下限**），`settleDrag` 只做 `Math.round(progress)`（`FLICK_COMMIT 2.6 格/秒` 仅在同向时多投一票、至多一格），然后**一次性**把 `current` 移动整个距离——⚠️ **这就是「落点即行程」**：拖过五盘就落在第五盘，落点是**位移**而不是方向；⚠️ 归一化 `current`（无论中途还是逐格）都会把幅度重新夹回一格，那正是这两条反馈的成因。⚠️ 环必须为这次行程重算：`ring ≥ n + 2·(span + DRAG_MAX)`，`MAX_FILLERS` 因此从 `4×MAX_SLOT_COUNT` 提到 `6×MAX_SLOT_COUNT`（24 张空壳）——`/projects` 环 15→**25**、`/lab` 22→**32**；只加 `DRAG_MAX` 不加环长，绘制带会走出环尾。同时**绘制带改成不对称**：`band = span − min(0, progress·sign(sink))`，否则向外拖时场外的空盘会被裁掉、行的边缘提前消失。实测（CDP、1700px、`SLOT_DRAG` 280px）：`/lab` 前拖 0.5/1/1.5/2/3/4/5 格 → 0/−1/−1/−2/−3/−4/−5，后拖 1/2/3/5 → +1/+2/+3/+5，两端越界 → 只走到档案尽头，不足半格 → 0；`/projects`（5 盘）1 格→−1、2 格→−2、更长 → 夹到 −2（档案尽头，不环绕）。② **「拖到空白磁带盒时…最近的有内容磁带盒直接刷新闪现出现，这样太僵硬了」**——落点始终夹到**真磁带**，但**下落点不夹**，所以拖过尽头时它落在布景里（可见的最多 `DRAG_MAX` 格让位）。⚠️ 当落点与真磁带不是同一格时，差的那几格是**滑**掉的而不是**赋值**掉的：`snap` tween 用 `WALK_BACK_MS 260` 把 `progress` 推到 `−step`，tween 结束时的那次 `recycle` 完成这次移动——读者看到的是空盘滑出、邻盘滑入。⚠️ 直接赋值＝整行一帧横跳，正是用户报的「闪现」。⚠️ 我试过四种「先退到空白再走回磁带」的写法，**全部放弃**：把空白当合法落点，弹簧就得先把整行推进一整格（那是它自己的行程），修正就成了反向的第二个整格，读起来是「被甩出去再被拽回来」，比没有过渡更糟；而且 `recycle` 在一个手势里 re-base 两次，正是它区间检查存在的意义。现在**哪一盘最近取决于拖过头多少**，这是行程模型的必然结果。实测：越过尽头 1 格 → `21:-1` 后 `20:0`；2.5 格 → 滑行到 `21:-2.10`（看得见的让位）再 `19:0`（260ms 内走回两盘）。③ **「light mode 切换到 dark mode 时，圆环覆盖过后，星空是直接刷新在画面上」**——浅色下 canvas 是 `display:none`，而**过渡救不了它**（过渡要求元素已在渲染中，`display:none` 没有可插值的起点），所以改成**关键帧动画** `sfArrive`（1.05s，透明度 0→0.85，外加 `brightness(0.55)` 提亮），由 `applyInstant` 在换主题**后一帧**加 `.sf-arrive`、`ARRIVE_MS 1100` 后摘掉（已在身上的 class 无法重放自己的动画）。⚠️ **只动 opacity，绝不动 transform**：这两个 canvas 有 `position: fixed` 的子元素，加了 transform 就会变成它们的包含块、整层背景位移。⚠️ 它必须在 `applyInstant` 而不是 `setRoot`：连点期间根停在 `NEUTRAL_THEME`，`setRoot` 触发时页面还被拷贝盖着，渐入会在拷贝后面烧掉。实测 opacity `0.35 → 0.63 → 0.74 → 0.806 → 0.85`，约 1.05s，结束后 class 已摘、两个播放器动画各自独立。④ **「开场动画太丑，改成 `输入/开场动画.html`…风格要偏现代+科幻+机能，而不是一味的复古风」**——`Layout.astro` 的开场标记 + `global.css §16` + `site-intro.ts` 整体重写：两片幕布像文件一样分开、**由外向内**填充的一对条纹量表（`clip-path: inset()` 读 `--reveal`）、`[NNN]` 计数 + 状态行、四角标签。⚠️ **刻意没搬的：扫描线、暗角、色差文字阴影**——那是参考里「复古终端」的那一半，这站的语言是机能仪表盘，照搬就是换戏服而不是统一风格；信号色一律取**当前 `--accent`**，不用参考写死的琥珀色（两主题都可读、开场和它盖住的页面同色）。⚠️ **进度模型是「爬升 + 逼近」**：`easeOutCubic` 在 `CLIMB_MS 3500` 内爬到 `FLOOR_CAP 85%`，之后**只在页面还没就绪时**按 `85 + (1 − e^(−t/APPROACH_MS)) × 14.95` 逼近（`APPROACH_MS 2400`，渐近线 99.95%，**永不达到 100%**）——这正是「加载好了就提前结束，没加载好就在接近百分百时慢一点等」的答案：数字一直在**动**，快到能读成进度、慢到能读成等待（实测 4s 87.8%、6s 94.7%）。⚠️ 更早的一版把计数**硬夹在 92% 停住**，那是「卡住」——数字冻住，读者看到的是一个已经放弃的东西。⚠️ 逼近必须用 `Math.exp(−已用时间)`，**不能除以剩余预算**：那会依赖帧率并在掉帧时跳变。就绪后剩下的部分在 `SETTLE_MS 260` 内冲完。⚠️ **`OPTIONAL_MS 2600` 与 `MAX_MS 4200` 分开是有意的**：黑洞视频和字体是「有更好」，文案和布局不是；为它们等满 4.2s，只会让状态从「等待」直接跳到结束——等到了一件永远不会来的东西。`MIN_MS 1100`、`OPEN_MS 820`，提前结束路径照旧走 `__rrsIntroGate`（书架的升架等它）。实测（真 `Page.reload`）：`html=is-intro`、量表 `8×900 @ x=0`、幕布 `1700×452`、`--intro-signal=#c07020`、`--intro-paper=#f1ece1`、计数 96px、状态 `Initialising…`、采样 `000 → 030 → 100`、结束自我移除 ✓。⚠️ 探针必须用 `Page.reload`（`navigation.type === "reload"` 才播开场），`Page.navigate` 是站内跳转、开场被跳过。见 §5.1、§5.2、§9 决策 22b/23、§11）
> - 2026-09-18 v1.23（**主题切换改成「整页拷贝的圆圈波」：圆里放真像素，不再填色**）：用户报「点击 light mode，圆会覆盖整个画面，画面黑屏，然后新界面突然从暗部里浮现…没有那种圆覆盖过后的丝滑过渡，圆和之后的浮现很割裂」，同时要求「按照最开始你设计的方案，但同时能支持多次点击产生多次圆」。**根因是方案本身**：v1.19/v1.20 的圆里填的是目标主题的**背景色**，所以每长一寸就把内容抹掉一寸，圆满屏时就是纯色，之后只能靠「撤掉覆盖层」把页面放回来——圆和浮现是两件事。定稿改成**每次都叠一份整页拷贝**（`document.body.cloneNode(true)`），圆里就是新主题的真实像素，最后一份盖满视口时同一帧撤掉所有拷贝（此时活动文档已经是同一主题），接缝为零。为此解决四个硬问题：① **`clip-path` 在克隆体上会不会把 `preserve-3d` 书架压平**——实测不会（拷贝与实时渲染逐像素一致），压平规则管的是「preserve-3d 元素自己在不在那一层里」，不是「上面隔了几层的祖先有没有 grouping 属性」；② **得给拷贝一个「根不是它」的主题**——`buildScope()` 把页面所有以 `:root` 开头的规则（含裸 `:root`，深色 token 在那）改写成 `.theme-scope…` 的运行时样式表，并且**一轮连点期间把根停在 dark**（唯一没有属性专属规则的主题），否则浅色根会把它的规则塞给深色拷贝（实测面板变浅色、|Δ| 107 → 修好后 1.0–2.0）；③ **落地那一帧必须关过渡**（`body` 自己有 400ms 的颜色过渡，不关就又变成「从旧主题淡入」）；④ **拷贝里的动画不能 pause**（拷贝插入时才启动动画，pause 冻在第 0 帧，书架磁带整片透明 |Δ| 37；`-3600s` 更糟 106；什么都不动 2.8 ✓）。另外：canvas 克隆是空的要拷位图、video 克隆会再开一个解码器要改成 poster、拷贝要跟滚动偏移、与停放主题相同的拷贝直接跳过。**验证**：解码截图逐像素比对四件事——拷贝 vs 同主题实时渲染（|Δ| ≤2）、揭示中途圆内 vs 新主题/圆外 vs 旧主题（1.26 / 0.00）、11×7 采样点每一帧都必须落在两个主题之一（77/77，「任何一帧都不是纯色」）、轮询到拷贝清空那一刻读 body 背景与在飞过渡数（0 个）。⚠️ 测试**不能 `cancel()` 圆的动画**（burst 靠 `finished` 收尾）。**已知代价**：每次点击约一个长帧（克隆+插入只要 1.3–3ms，长的是浏览器给 ~500 节点做 layout+paint；平均帧时间 +1ms 不变）。六个页面形态（首页视频/notes 卡片/about/另一语言书架/详情页）全部冒烟通过。见 §9 决策 23、§6.1、§11）
> - 2026-09-15 v1.13（两个新条目按用户指示精修，三语言同步）：① 项目统称 **"HSV tests"**（en/zh/nl 均不得叫 "HSV station/色彩台/HSV-station"）；② 1.3" SH1106 OLED + EC11 是**一体化模块**，硬件清单按一件计；③ 0914 删掉"坑6 GPIO 32/33 编码器信号不稳"（用户确认该问题不存在，属 AI 源文虚构）；④ 删掉 "tcaSelect err=5 → 把 RST 接 3.3V" 的建议（RST 悬空即可，出问题先查 SDA/SCL 与杜邦线）；⑤ 0914 开头定位句改为"多路复用器省引脚、避免引脚不够用"；⑥ 引脚汇总表写明 **TRA→GPIO x，TRB→GPIO y**（用户要求小白能照着做，不再写 "25, 18" 这种模糊写法）；⑦ TRA/TRB 不能走 I2C、省引脚得换 I2C GPIO 扩展芯片（MCP23017）的说明保留并写明确；⑧ 0915 开头混乱的 ASCII 架构图换成与 A4988 接线一致的**纯文本句**（非代码块）："ESP32 到 A4988 的接线：GPIO 12 ── DIRECTION，…"。用户拒绝联网搜索（本条目原稿自带 MCP23017 内容，未再补充网络资料））
> - 2026-09-15 v1.14（0915 条目图表再简化，三语言同步）：接线段的 fenced 代码块改列表后发现与上方表格**完全重复**，最终**整段删除**（表格已含全部接法，接线信息只保留开头的一句文本链："ESP32 到 A4988 的接线：GPIO 12 ── DIRECTION，…"）；"当前系统架构"的整段 box-drawing 大图删除，换成一行文本链："ESP32 ── TCA9548A 多路复用器 ── 3 个 OLED + EC11 一体化模块；另一侧 ESP32 ── A4988 电机驱动器 ── 42 步进电机"（电机为 **42** 步进电机，用户新提供的事实）。**新约定**：用户明确不喜欢 box-drawing ASCII 架构图与接线代码块，新条目里这类信息用一行文本链（A ── B ── C）表达，且不与表格重复）
> - 2026-09-10 v1.11（**生产站卡片毛玻璃失效修复**：Vite 8 的 CSS 压缩器 lightningcss 把 `-webkit-backdrop-filter` 当作 `backdrop-filter` 的别名，同一条规则里成对出现即合并、**保留最后一条**；`ProjectCard.astro` 原本「无前缀在前、前缀在后」→ 产物只剩 `-webkit-` 版，而 Chrome/Edge/Firefox 都不认前缀版（headless 实测对照），于是生产站没有毛玻璃、dev（不压缩）却正常——**不是部署/缓存问题**（产物逐字节比对过）。修法：无前缀那条留在 `.card`，Safari ≤17 的兜底挪进紧随的独立 `@supports (-webkit-backdrop-filter: …)` 规则，一条规则一个声明，压缩器无从合并（已验产物两条并存）。见 §9 决策 22、§11）
> - 2026-09-10 v1.10（引力透镜**视口自适应默认值**：§10 的 `--lens-*` 基线原为 2K 屏（视口 1661×802）单点烘焙，窗口拖到 1080p 屏（1234×562）后黑洞圈/中心与视频错位。因视频盒 125%+cover，凡宽高比 ≥16:9 的窗口视频特征都随**视口宽 W** 缩放，两屏手调档位完全符合同一律 → 对 7 个视频锚定旋钮（`cyVh`、`r-outer/r-mid/r-inner`、`r-ring`、`disk-inner-w/h`）做 px∝W 两点线性拟合（锚 A = 发货基线 = §10/FALLBACK，锚 B = 用户 1080p 实测档，组件 §2a `SCALED` 表）；CSS 恰在发货基线时自动按当前视口取值（≥16:9 才生效，其余保基线），tuner inline 值或 ≠FALLBACK 的手改 §10 逐字冻结（按来源不按数值判定）；`measure()` 每次量完 W/H 重读，拖窗换屏自动重拟合；HUD/COPY/nudge 走同一 effective 路径，COPY 含拟合值时输出 ⚠ 静态快照提示；localStorage 键 v6→v7 且**只存被调过的增量旋钮**（v6 全量快照会把某屏数字冻结到所有屏）。重新基线化 = global.css §10 与组件 FALLBACK/SCALED 两处同步改。见 §9 决策 20/21、§11）
> - 2026-09-10 v1.9（引力透镜星空：新增 `src/components/GravitationalLens.astro` + `#lens-canvas`（样式在 global.css §10），只在三个首页、暗色、桌面端（≥769px）渲染并接管 `#starfield-canvas`。星空为**单股连续内流**（发布当日重构，弃分层独立循环）：每颗星自外场边缘出生后穿三档速度分段（外层极慢 → 中层快 → 内层越靠近黑洞越慢）单调内落，途中被吸积盘遮住时继续内落、离开盘内缘后重新露出，直到黑洞外圈淡出后重生回外场；`--lens-r-ring` 以内**不绘制任何星星**（纯黑，视频黑洞透出）。所有参数都是 `#lens-canvas` 上的无单位自定义属性；自带键盘调参工具（反引号或 `?lens=tune`：方向键调每个圈半径与黑洞中心，`C` 复制成 CSS，`R` 复位），调参只存浏览器 localStorage。增补：顺时针旋进（∝ rRing/r）、吸积盘遮罩环带（`--lens-disk-inner/outer`）、Shift ×10 / Alt ×0.1 粗调细调；默认值按用户 1661×802 实测烘焙；吸积盘遮罩升级为可旋转/压扁/二维移动的椭圆环 + 边缘羽化；方向键改为 ←→ 横轴 / ↑↓ 纵轴；调参窗改磨砂玻璃 + 可拖拽（限制在窗口内）+ 细滚动条 + en/zh/nl 本地化，导航栏齿轮入口带缓入动画；吸积盘内外边缘各自独立调横向/纵向半轴（`-inner-w/h`、`-outer-w/h`）；默认值按用户 1661×802 实测更新，新增 `--lens-gap-boost` 提亮「盘内边→黑洞圈」之间的星；再增 `--lens-photon-band` / `--lens-photon-size`：黑洞圈外一圈**圆形**（不做切向拉伸）、更小更亮的密集小星星带（调参键升到 v6）。见 §9 决策 20、§10、§11）

> - 最后更新：2026-09-09 v1.8（首页黑洞视频背景层：`输入/黑洞动画 - AI 插帧.mp4` 转码未改、原样拷贝为 `public/media/blackhole.mp4`，由 `Layout` 的 `blackhole` prop 在**仅三个首页**（en/zh/nl）渲染 `#blackhole-layer` + `#blackhole-video`；首页暗色模式叠层固定为「纯黑底 → 黑洞视频 → 星空 → 页面内容」，其他页面不受影响；位置/尺寸在 global.css §10 用 `--blackhole-*` 变量调节，`--blackhole-y` 按视口高度线性跟随（`calc(122px + 37.8vh)`，由用户实测两点拟合），且**只在桌面端（≥769px）启用、手机端连视频都不下载**；首帧解码后渐入避免闪跳，§6.3/§9 决策 18/§11）
> - 2026-09-04 v1.7（① 背景粒子神经网络被 3D 星空暂时替换：`#starfield-canvas` 天球式拟真星空 + 鼠标拖拽 + 真实恒星光谱色，原 `#neural-canvas` 与早期 2D 星空脚本保留可回退，§6.3/§10/§11；② 全站 URL 尾斜杠约定：`trailingSlash: "always"` + 所有链接/sitemap 带尾斜杠，修复 GSC"网页会自动重定向"告警，§9 决策 16；③ 正文图片构建时解析：新增 `markdown-resolve-images.ts` Sätteri 插件接入 Astro 原生内容图片管线，删除 ProjectDetail 客户端图片修正，修复 Cloudflare 4xx 报告中的详情页图片 404，§9 决策 17）
> - 维护规则见 §12：代码若与本文件冲突，**以代码为准**，并更新本文件。
>
> **⚠️ v1.2 全量校正（旧段落中未逐行改写的“双语/仅 zh”表述，一律以下面为准）：**
> 站点现在是 **en（无前缀）/ zh（`/zh`）/ nl（`/nl`）三语**；`src/content/entries` 每个文件夹为 `en.md + cn.md + nl.md + 图片`；`src/pages` 有完整的 zh 与 nl 静态镜像；两个动态详情路由分别是 en 与“所有非 en 语言”。`translations.ts` 为 `{en, zh, nl}`。`package.json` 新增 `npm run check`（依赖 `@astrojs/check` + `typescript` devDeps）。`Layout` 的 prop 已改为 `alternateHrefs`（`undefined`=自动假设全部语言；空数组=显式无译文；`null`=省略 hreflang）。404 页的导航语言切换指向各语言首页。`ArtGallery.astro` 已删除，两个详情模板不再有 art 分支；`gallery` schema 字段暂时保留但当前无消费者。art 页面改为视觉冻结：允许代码优化，但视觉不得变化；`npm run optimize-art` 生成 `public/art-optimized/` 无损 webp（每张输出必须 ≥ 原文件 50%，否则保留原图），共享发现逻辑在 `src/utils/artImages.ts`。卡片与详情封面通过 `astro:assets` 输出 webp。

---

## 1. 项目概览

| 项       | 内容                                                                                                                                                                                        |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 定位     | RrSuika Studio 个人作品集网站：工业设计 × 嵌入式系统 × 创意制造（Industrial Design × Embedded Systems × Creative Making）                                                                   |
| 技术栈   | **Astro 7.1.6**（纯静态输出）、**zod 4**（内容 schema）、TypeScript（`astro/tsconfigs/strict`）、原生 CSS + 少量原生 JS。**无任何集成**：无 React/Vue/Svelte、无 MDX、无 Tailwind、无适配器 |
| 站点     | https://rrsuika-studio.pages.dev （Cloudflare Pages，push 到 `main` 自动部署）                                                                                                              |
| 语言     | 英文为主（默认无前缀），中文为 `/zh` 前缀镜像。**手动 i18n**：不使用 Astro 内置 i18n routing（虽然 astro.config.mjs 里声明了 i18n 配置，实际路由全部手写，见 §3、§9）                       |
| 设计方向 | 复古科幻 CRT 终端 / 技术手册 HUD 风（retro-futurism）：暗色默认 + 亮色"蓝图纸"（Soviet technical manual）主题；2026-09-04 暗色文字改为暖白复古色，亮色背景改为黑色四边形+十字网格                                                                               |
| 阶段     | 生产运行中。架构稳定；art 页面**冻结**（用户决定）；首页 Hero 终端处于**临时隐藏实验**状态（§14，见 §10）                                                                                   |

**GitHub**：远程仓库 `RrS-Site`（账号 RrSuika；本地文件夹名为 MyPortfolio），主分支 `main`，工作树干净即代表生产状态。

---

## 2. 项目目录结构

```text
MyPortfolio/
├── CLAUDE.md                  # 项目规则/政策（设计系统、架构约定、SEO 约定、冻结声明）
├── AGENTS.md                  # 给 AI 的开发服务器工作方式（astro dev --background 等）
├── AI_CONTEXT.md              # ← 本文件
├── astro.config.mjs           # site URL + i18n 声明（详见 §8）
├── package.json               # scripts + 依赖（astro、zod）
├── tsconfig.json              # extends astro/tsconfigs/strict
├── README.md                  # 未修改的 Astro minimal 模板 README（见 §10）
├── 启动.bat                   # 一行：cmd /k npm run dev
├── .vscode/                   # 推荐 astro 插件 + "Development server" 启动配置
├── scripts/
│   └── og-card-gen.mjs        # 重新生成 public/og-card.png 的脚本（手动运行）
├── public/                    # 原样拷贝进站点的静态资源（不参与打包）
│   ├── favicon.ico / favicon-32x32.png / favicon-48x48.png
│   ├── og-card.png            # 默认 og:image（1200×630，由 scripts/og-card-gen.mjs 生成）
│   ├── robots.txt             # 全站允许；AI 训练爬虫禁 /art/ 与 /zh/art/
│   ├── google0d89945c0c4db4b1.html  # Google Search Console 验证文件
│   ├── icons/                 # 6 个软件 logo PNG（about 档案卡 SOFTWARE 行；来自 输入/，勿重命名）
│   ├── media/blackhole.mp4    # 首页黑洞背景视频（1920×1080/60fps/约5s/3.1MB，来自 输入/黑洞动画 - AI 插帧.mp4，未转码）
│   └── art/fashion-design/    # 4 张 PNG 副本;为保留 alpha 透明通道直接以原图提供
├── src/
│   ├── content.config.ts      # ★ 内容 Schema 唯一事实来源（单一 collection `entries`）
│   ├── content/entries/       # ★ 全部内容：23 个条目文件夹，各含 en.md + cn.md + 图片
│   ├── layouts/
│   │   └── Layout.astro       # ★ 全站唯一布局（head/SEO/主题/背景层/全局脚本）
│   ├── pages/                 # 路由（静态页 + zh 镜像 + 2 个动态路由 + sitemap + 404）
│   │   ├── [type]/[...slug]/index.astro   # en 详情页动态路由
│   │   ├── [lang]/[type]/[slug]/index.astro  # zh 详情页动态路由
│   │   └── zh/                # 全部中文静态页镜像（独立文件，非模板共享）
│   ├── components/            # 12 个组件：全局 6 个（含 GravitationalLens 引力透镜）+ 列表/详情 4 个（含 CassetteShelf）+ home/ 5 个（见 §5）
│   ├── styles/
│   │   └── global.css         # ★ 设计系统唯一事实来源（14 个 section，1070+ 行）
│   └── utils/                 # 4 个工具模块（见下表）
├── 输入/                      # ★ 用户给 AI 的"投递箱"：素材放这里，AI 读取后转写为站内内容（§12 规则 13）。已移出版本控制，永不提交/上传
├── node_modules/ .astro/ dist/  # 依赖/构建产物（gitignored，勿读勿改）
```

**`src/utils/` 四个工具模块（都读全了，是小型文件）：**

| 文件              | 职责                                                                                                                                      | 关键导出                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `routes.ts`       | **详情 URL 单一事实来源**                                                                                                                 | `typeToRoute`（projects→projects, lab→lab, **note→note 单数**, art→art）、`buildEntryUrl(entry, language)`                                                                                                                                                                                                                                                                                                                              |
| `i18n.ts`         | 语言判定与路径换算                                                                                                                        | `Language` 类型、`defaultLanguage="en"`、`getLanguageFromPath`（`/zh` 前缀判定）、`getLocalizedPath`（加/剥 `/zh` 前缀）、`findTranslation`（按 `translationKey`+`lang` 配对译文条目）                                                                                                                                                                                                                                                  |
| `translations.ts` | **全部文案唯一事实来源**（14 个 section：meta/nav/common/language/card/hero/featured/explore/latest/aboutPreview/footer/sections/system/skillMap） | `translations`（`{en, zh}`）、`getTranslations(language)`；用法 `const t = getTranslations(lang)` 后按路径取文案；`skillMap.hubs` 为 about 页技能星图数据（name + skills 数组，按 index 对应页面脚本里的坐标/连线）                                                                                                                                                                                              |
| `images.ts`       | 内容图片解析                                                                                                                              | `getProjectImage(id, filename)`：`import.meta.glob(eager)` 扫描 entries 图片；**特例**：`fashion-design` 的 PNG 返回 `/art/fashion-design/{file}`（走 public 保留透明通道）。**注意**：Astro 图片 glob 运行时返回的是 ImageMetadata 对象（`.src` 才是 URL），`getProjectImage` 返回原始值（ArtGallery 冻结区与详情页模板自行归一化）；渲染原生 `<img>` 时用 `getProjectImageUrl`（归一化为 URL 字符串，ProjectCard/ProjectDetail 在用） |
| `markdown-resolve-images.ts` | 正文图片构建时解析（Sätteri raw 节点插件，§9 决策 17） | `resolveEntryImages`：把正文原始 HTML 里 `<img src="./…">` 重写为 `__ASTRO_IMAGE_` 标记并注册进 `localImagePaths`，由 Astro 原生管线在渲染期解析为哈希 URL。经 `astro.config.mjs` 的 `markdown.processor: satteri({ hastPlugins })` 注册。⚠️ 勿在插件内用 `images.ts` 的 glob **值**（配置 bundle 无 astro:assets 处理，`.src` 是原始 `/src/content/...` 路径，生产 404）；只可用 lazy glob 的**键**做存在性检查 |

**在哪里改什么：**

- 新增页面 → `src/pages/`（en）+ `src/pages/zh/`（zh 镜像，两语言都要建）
- 新增组件 → `src/components/`（页面专用放对应页面内联或 `home/` 子目录）
- 修改全局样式/token → `src/styles/global.css`（唯一全局样式文件，遵守 fidelity 政策 §6）
- 新增内容 → `src/content/entries/`（§4）
- 改 Schema → `src/content.config.ts`
- 改路由映射 → `src/utils/routes.ts`（**不要**在组件里自建映射，见 §10）
- 改文案 → `src/utils/translations.ts`
- 站点级配置 → `astro.config.mjs` / `package.json`

---

## 3. 页面与路由架构

### 3.1 路由总表

| 路由                                        | 文件                                                   | 说明                                                              |
| ------------------------------------------- | ------------------------------------------------------ | ----------------------------------------------------------------- |
| `/`                                         | `src/pages/index.astro`                                | 首页（en）                                                        |
| `/zh`                                       | `src/pages/zh/index.astro`                             | 首页（zh，独立文件）                                              |
| `/projects` `/lab` `/notes` `/art` `/about` | `src/pages/{projects,lab,notes,art,about}/index.astro` | 五个列表/静态页（en）                                             |
| `/zh/projects` … `/zh/about`                | `src/pages/zh/{...}/index.astro`                       | 五个 zh 镜像（独立文件）                                          |
| `/{type}/{slug}`                            | `src/pages/[type]/[...slug]/index.astro`               | **en 详情页**：`/projects/{slug}`、`/lab/{slug}`、`/note/{slug}`  |
| `/zh/{type}/{slug}`                         | `src/pages/[lang]/[type]/[slug]/index.astro`           | **zh 详情页**（`lang` 参数**硬编码为 `"zh"`**，此文件只服务中文） |
| `/sitemap.xml`                              | `src/pages/sitemap.xml.ts`                             | 手写 XML 端点                                                     |
| `*`（未命中）                               | `src/pages/404.astro`                                  | 输出 `/404.html`，Cloudflare Pages 兜底所有未匹配路由             |

### 3.2 关键路由规则（容易踩坑）

1. **`note` 详情是单数 `/note/{slug}`，列表是复数 `/notes`**。历史上一份发散的路由映射生成过 `/notes/{slug}` 造成 404;`src/utils/routes.ts` 顶部注释专门记录此教训，所有链接必须经 `buildEntryUrl`/`typeToRoute`。
2. **两个动态路由文件各管一种语言，互不重定向**：en 路由用 `[...slug]` rest 参数（当前只用到单段，rest 是为了兼容未来多段 id）；zh 路由的 `[lang]` 段实际只会是 `"zh"`（en 路径由另一个文件接管，避免了 `/en/...` 前缀）。
3. **art 类型没有详情页**：两个动态路由的 `getStaticPaths` 都过滤 `entry.data.type !== "art"`，sitemap 也排除 art。art 只在 `/art` 列表页以自扫描方式展示（见 §7.7）。
4. 详情页 `getStaticPaths` 的核心逻辑（两个文件对称）：

```js
const entries = await getCollection("entries");
// en 路由: filter lang==="en"；zh 路由: filter lang==="zh"；两者都排除 type==="art"
const slug = entry.id.split("/")[0]; // 文件夹名即 slug
const translation = findTranslation(entries, entry, "zh" | "en");
// props: { project, projectId: slug, translation }
```

5. 详情页模板里存在 `project.data.type === "art" ? <ArtGallery/> : <ProjectDetail/>` 分支，但 art 已被过滤，**该分支实际不可达**（防御性代码）。渲染永远走 `<ProjectDetail project projectId Content translation />`。
6. 详情页内嵌 **TechArticle JSON-LD**（两张模板里各有一份，不在组件内）：`datePublished`/`dateModified` 都用 `data.date.toISOString()`（完整 ISO-8601 带时区；无独立修改日期，回退发布日期）；`translationOfWork` 嵌套对方语言文章的元数据（有译文时）。
7. `sitemap.xml.ts`：6 个静态路径 × 3 语言（en/zh/nl）+ 每个非 art 条目按其 `lang` 生成 URL（`/{type}/{slug}/` 或 `/zh|nl/{type}/{slug}/`），`lastmod` = 条目 `date` 的 YYYY-MM-DD，按字典序排序。**所有 URL 带尾斜杠**（§9 决策 17）。
8. `404.astro`：无语言判定，固定双语混合内容（三个按钮：`/`、`/zh`、`/projects`），`noIndex`。

### 3.3 新增内容类型 / 页面时需要动的地方

- 新**条目**（不新增类型）：只需加内容文件（§4），路由自动生成。
- 新**type 枚举值**（很少需要，代价高）：`src/content.config.ts` 的 enum → `src/utils/routes.ts` 的 `typeToRoute` → 两个动态路由的过滤逻辑 → `sitemap.xml.ts` → 相应列表页 →（zh 镜像同步）。
- 新**静态页面**：en 文件 + zh 镜像文件两个都要建，Layout 传 `alternateHref`（见 §5.1）。

---

## 4. 内容管理架构

### 4.1 单一 Content Collection：`entries`

- 定义：`src/content.config.ts`（55 行，全文件即 schema）。
- Loader：`glob({ pattern: "**/*.md", base: "./src/content/entries" })`。
- **URL slug = 条目文件夹名**（`entry.id` 形如 `{文件夹名}/en`，代码里 `entry.id.split("/")[0]` 取 slug）。
- 每个条目文件夹 = `en.md` + `cn.md` + `nl.md` + 同目录图片。**25/25 个文件夹都有 en/zh/nl 三语言文件，无缺失。**

### 4.2 Schema 全部字段（`src/content.config.ts` 逐字为准）

| 字段             | 类型                                     | 必填 | 作用 / 谁消费                                                                                                                                                                                                               |
| ---------------- | ---------------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `title`          | string                                   | ✓    | 标题；列表卡片、详情 hero、JSON-LD、`<title>`                                                                                                                                                                               |
| `date`           | `z.coerce.date()`                        | ✓    | 发布日期；排序、卡片/详情显示、sitemap lastmod、JSON-LD（`.toISOString()`）                                                                                                                                                 |
| `description`    | string                                   | 否   | 摘要；卡片、详情 hero、`<meta description>`、JSON-LD                                                                                                                                                                        |
| `cover`          | **string**（无 `image()` 校验）          | 否   | 封面图**裸文件名**（如 `cover.png`、`01-setup.png`），相对条目文件夹；由 `getProjectImage(slug, cover)` 解析。18 个条目有，5 个 note 没有（button-debounce、isolation-gpio、power-supply、signal-amplifier、signal-filter） |
| `type`           | enum `["projects","lab","note","art"]`   | ✓    | 决定列表页归属与详情 URL 段。**注意是 `projects` 不是 `work`**；`gallery` 不是 type 而是字段                                                                                                                                |
| `category`       | string                                   | 否   | 细分分类；卡片底栏与详情 info 格                                                                                                                                                                                            |
| `collaboration`  | string                                   | 否   | 非空即显示 ProjectCard 的 TEAM badge（`/TEAMWORK/` 或 `/协作/`）。当前仅 `zoem-bike-bakfiets` 有值 `team`                                                                                                                   |
| `tags`           | string[]                                 | 否   | 卡片标签、列表页筛选 chips（`#{tag}` 显示，3n 循环红/黄/青描边）                                                                                                                                                            |
| `tools`          | string[]                                 | 否   | 详情页 info 四格之一                                                                                                                                                                                                        |
| `featured`       | boolean                                  | 否   | 目前**不参与**任何自动筛选（首页精选用硬编码 `featuredKeys`，见 §7.2）；保留字段                                                                                                                                            |
| `gallery`        | array of `{file: string, title: string}` | 否   | 仅 4 个 art 条目使用（fashion-design 4 项、food-art 6 项、illustrations 8 项、product-posters 7 项），由 **ArtGallery 组件（冻结）** 消费                                                                                   |
| `lang`           | enum `["en","zh"]`                       | ✓    | 条目的语言，决定挂到哪个语言的路由/列表                                                                                                                                                                                     |
| `translationKey` | string                                   | ✓    | en/cn 配对键（通常=文件夹名；**唯一例外** zoem-bike 文件夹用 `zoem-bike-cargo-box`）                                                                                                                                        |

### 4.3 条目清单（25 个，按 type 统计）

| type       | 数量 | 条目                                                                                                                                                                                                                                                                         |
| ---------- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `lab`      | 12   | 3d-printing-abs-material-test、esp32-a4988-stepper-motor、esp32-ec11-encoder-oled、esp32-ec11-stepper-motor、esp32-inmp441-noise-monitor、esp32-multi-oled-encoder-hsv、esp32-rgbww-color-wheel、esp32-rgbww-fcob-comm、esp32-serial-test、esp32-wifi-led-brightness-control、studio-electrical-optimization、tinkercad-3ch-led-strip |
| `note`     | 6    | button-debounce-pullup-pulldown、electronics-fundamentals-notes、isolation-gpio-driver-mosfet-bjt-relay-optocoupler、power-supply-ldo-buck-boost、signal-amplifier-opamp-comparator、signal-filter-rc-high-low-pass                                                          |
| `art`      | 4    | fashion-design、food-art、illustrations、product-posters                                                                                                                                                                                                                     |
| `projects` | 3    | body-armor-protective-vest、light-diffusion-test-platform、zoem-bike-bakfiets                                                                                                                                                                                                |

### 4.4 en/cn 配对与 frontmatter 惯例

- **两语言相同**：`type`、`date`、`cover`、`featured`、`translationKey`、`collaboration`、`gallery[].file`。
- **两语言不同**：`title`、`description`、`category`、`lang`；`tags`/`tools` 多数翻译（个别条目两语言完全相同，如 esp32-rgbww-color-wheel 的 tags）；art 条目 `gallery[].title` 各自翻译。
- 图片路径：frontmatter 里写**裸文件名**（`cover: cover.png`）；正文 markdown 里用 `./` 前缀（`![...](./xxx.png)`）。
- 图片命名惯例：专用封面 `cover.png`（7 个文件夹）；`01-描述.png` 编号+描述（多数）；纯数字（studio-electrical 的 1–16.png）；扩展名 .png/.jpg/.jpeg 混用属正常。

### 4.5 如何新增内容（Step-by-step）

**新增项目（projects）/ 实验室记录（lab）：**

1. 建文件夹 `src/content/entries/{kebab-case-slug}/`（slug 即 URL，多语言共用）。
2. 写 `en.md`（`lang: en`）、`cn.md`（`lang: zh`）与 `nl.md`（`lang: nl`），三个文件 `translationKey` 相同（通常=文件夹名）。
3. frontmatter 必填：`title`、`date`、`type`、`lang`、`translationKey`；建议：`description`、`category`、`tags`、`tools`、`cover`。
4. 图片放同文件夹，frontmatter 引用裸文件名。
5. 完成;路由、列表页、sitemap、首页 SYS.LOG 全部自动生效（首页精选除外，需手动加 `featuredKeys`，§7.2）。

**新增学习笔记（note）：** 同上，`type: note`。注意封面可选（5 个老笔记就没有 cover），TOC 对 note 取 h2 级别标题（§7.4）。

**新增艺术条目（art）：** 目前 art 条目 4 个已与冻结的 `/art` 页面硬编码分类绑定（§7.7、§10），**新增 art 条目需要先与用户确认**;冻结政策只允许改 SEO 相关 frontmatter。

> 素材来源：用户可能把图片/文档放在 `输入/` 里让 AI 转写（见 §12 规则 13）。

---

## 5. Component / Layout 架构

### 5.1 Layout.astro（全站唯一布局）

Props（都在 `src/layouts/Layout.astro` 顶部 interface）：

| Prop             | 语义                                                                                                                                                 |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `title?`         | `<title>`/og:title，默认 `"RrSuika Studio"`                                                                                                          |
| `description?`   | 缺省回退 `t.meta.description`（按语言）                                                                                                              |
| `ogImage?`       | string 或 null；缺省回退 `/og-card.png`                                                                                                              |
| `alternateHref?` | **`undefined` = 假定对方语言页面存在**，自动用 `getLocalizedPath` 推导 hreflang；**`null` = 不输出 hreflang**（页面无译文时，详情页无译文时传 null） |
| `noIndex?`       | 输出 `robots noindex`（404 页用）                                                                                                                    |
| `blackhole?`     | 布尔；true 时渲染首页黑洞视频层（`#blackhole-layer` + `#blackhole-video`，暗色模式专用）。**仅三个首页**（`index.astro`/`zh/index.astro`/`nl/index.astro`）传 `blackhole`，其他页面不传 |

Layout 负责：head 全套（主题守卫内联脚本→防闪烁、charset/viewport/color-scheme/theme-color×2、Google Fonts JetBrains Mono 400/700/800/900、favicon、canonical、hreflang+x-default、og:_/twitter:_、**JSON-LD WebSite** `{name:"RrSuika Studio", url, inLanguage:["en","zh"]}`）；body 结构（skip-link、`#mobile-notice` 移动端提示、**`#blackhole-layer` 黑洞视频层（仅首页）**、`#star-canvas`（停用）、`#starfield-canvas` 星空画布、**`#lens-canvas` 引力透镜星空（仅首页/暗色/桌面端，接管旧星空）**、`#neural-canvas`（停用）、`<Navbar/>` + `<main#main-content/>`(slot) + `<Footer/>`、`<ScrollMeter/>`、`#crt-overlay` + `#tube-vignette` 背景层、黑洞自动播放/星空/主题切换/mobile-notice 脚本）；**站点开场**（`<head>` 内的 **SITE INTRO GUARD** 内联脚本决定放不放并持有 `window.__rrsIntroGate/__rrsIntroResolve` + `src/scripts/site-intro.ts` 播视觉 + `global.css §16`，规格与门控见 §9 决策 23 与 §11）。`global.css` 在此以 frontmatter import 引入（唯一引入点）。

### 5.2 组件清单（共 12 个）与复用指引

**全局复用（在 Layout 内，全站生效）：**

| 组件                | 职责                                                                                                                                                                                                                           | 备注                                                                |
| ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------- |
| `Navbar.astro`      | sticky 顶栏：品牌三色点、6 个导航链接（`t.nav.*`，href 用 `getLocalizedPath`）、**语言切换=镜像链接**（详情页先查译文是否存在，无则拦截弹 toast，见 §7.1）、主题切换按钮（调 `window.__toggleTheme`）、`#clock` 时钟、警告条纹 | **无汉堡菜单、无当前页高亮（无 aria-current）**；≤1000px 变纵向堆叠 |
| `Footer.astro`      | 页脚：三色条纹+状态行、身份区块、链接（GitHub `RrSuika`、email `ranrsuika@gmail.com`、pixiv `users/71884225`、QQ、Discord）、条形码、版权                                                                                      | 硬编码 `ROTTERDAM // NL`、`2026.V9.19`（每次大改手动升版，2026-09-19 由 `2026.V8.16` 升上来）                               |
| `ScrollMeter.astro` | 右缘 LED 滚动进度条（10 段）+ 回顶/回底按钮 + 轨道拖拽滚动                                                                                                                                                                     | rAF 节流、`aria-hidden` 计量表、≤768px 隐藏                         |
| `GravitationalLens.astro` | 首页引力透镜星空 `#lens-canvas`：单股连续内流（外慢→中快→内缓三档速度段，非分层循环）+ 顺时针旋进 + 吸积盘椭圆遮罩 + 黑洞圈外**圆形小星星带** + 键盘调参 HUD（详见 §9 决策 20、global.css §10 的 `--lens-*`） | 仅首页 / 暗色 / 桌面端（≥769px）；由 Layout 的 `blackhole` prop 渲染，并接管 `#starfield-canvas` |

**列表/详情复用：**

| 组件                  | Props                                                  | 职责                                                                                                                                                                                                                          | 被谁用                                                                             |
| --------------------- | ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------- |
| `ProjectCard.astro`   | `project`（必填）                                      | 条目卡片（整卡是 `<a>`）：PRJ_NODE//TYPE 头、cover+IMAGE_STREAM 角标、**TEAM badge（collaboration 驱动）**、title/description/tags、category+date 底栏                                                                        | **仅**首页 FeaturedProjects（2026-09-18 起 `/projects`、`/lab` 改用 CassetteShelf）。链接经 `buildEntryUrl`（单一来源） |
| `CassetteShelf.astro` | `entries`、`kind`（`"projects"｜"lab"`）、`filterTags`、`title`、`description`（必填） | **projects / lab 列表页的整套体验**（2026-09-18，同日多轮打磨，最新见 v1.32）：页头 + 标签筛选 + CSS-3D 磁带盒书架舞台。磁带盒 = 6 个 `.cas-f` 面（**封面铺满整个盒面**，转盘/编号/日期/色条/标题浮在其上 / 左右书脊 / 上下底 / 背板），姿态数学 1:1 移植参考 `layoutPose()`；**一屏 9 盘**（`MAX_SLOTS 4` + 7 个完整 + 两侧渐隐，主角占画面宽约 28%、盘距 0.45 自身宽），不足的位用空磁带布景（`.cas-blank`，`MAX_FILLERS = 6×MAX_SLOT_COUNT`）补满，`walkFrom/walkTo` 只覆盖真磁带所以空白永不可选；**主角落在环上第 `half` 位**才真正居中（这是最容易错的一步）；换盘＝**一条缓动 tween 驱动全行**（`progress` 共享，`slotIndex = ring − current + progress`），落地后环跳一格；开场＝黑幕渐入 → 从下方**左→右**升架（X 锁死、全程侧身、scale 2.05→2.30）→ 中心磁带 spring 抽出转 15°；交互＝左右拖动（**落点即行程**：`dragOffset` 是**整段手势**的位移，`SLOT_DRAG() = clamp(170px, 17vw, 280px)` 买一格，`progress` 就是格数偏移——手势期间不设上下限、只由 `DRAG_MAX 5` 格封顶；松手取整决定落点、`FLICK_COMMIT 2.6 格/秒` 只作**同向加一格**的加票，然后 `current` **一次性**移动整个距离；落点夹到真磁带，若落在布景里则用 `WALK_BACK_MS 260` 的 `snap` 把差值**滑**回去）、方向键、横向滚轮、两端**无圆框的粗 V 形箭头**（每侧一条写死方向的 SVG 折线：prev `M12 2 L2 13 L12 24` 尖在左、next `M2 2 L2 13 L2 24` 尖在右——⚠️ **折线的尖是中间那个坐标**，别再按首坐标推方向；尖角靠 `stroke-linejoin: miter`）、点击主角或面板里的 `INSPECT FILE` 实心按钮过场进详情页；右侧磨砂面板显示当前条目简介（WIP 琥珀 / ONLINE 绿，点由 CSS 画）。**无 three.js 依赖**，纯 `preserve-3d`；`≤860px`/`reduced-motion`/不支持 3D ⇒ flat 平面卡片模式 | `/projects`、`/lab` 及 zh/nl 镜像共 6 页 |
| `ProjectDetail.astro` | `project`、`projectId`、`Content` 必填；`translation?` | 详情主体：hero、info 四格（年份/类型/分类/工具）、tags、正文 `<Content/>`、**侧边 TOC**、翻译不可用提示；客户端 JS：图片全屏灯箱、process 滚动轨道、TOC 生成（note 取 h2，其余取 h1，剥数字编号前缀）。正文 `./` 图片的解析已移到构建期（`markdown-resolve-images.ts`），组件内不再有图片路径修正 | 两张详情页模板                                                                     |
| `ArtGallery.astro`    | `project`、`projectId`                                 | **冻结**：art 条目详情展示（masonry 三列 + lightbox），消费 `gallery` 字段                                                                                                                                                    | 详情模板 art 分支（**实际不可达**，见 §3.2-5）                                     |

**首页专用（`src/components/home/`，index.astro、zh/index.astro、nl/index.astro 用）。页面区块顺序：Hero → FeaturedProjects → ExploreLinks → AboutPreview → LatestUpdates（2026-08-16 起 AboutPreview 与 LatestUpdates 互换）**：

| 组件                     | 职责                                                                                                                                                                                                                   |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Hero.astro`             | 首屏：左列大标题（Latin 保留 display tracking）+ 描述 + CTA；右侧 SYS.BOOT 打字机终端（**被 global.css §14 隐藏，元素完好**）                                                                                          |
| `FeaturedProjects.astro` | 精选区块：接收 `projects` 与 `totalCount`，渲染 3 张 ProjectCard（编号 01/02/03 叠加）                                                                                                                                 |
| `LatestUpdates.astro`    | SYS.LOG 面板：最新条目列表，**正确使用 `buildEntryUrl`** 生成链接                                                                                                                                                      |
| `ExploreLinks.astro`     | 4 张模块卡（PROJECTS/LAB/ART/NOTES）链到各列表页                                                                                                                                                                       |
| `AboutPreview.astro`     | OPERATOR 身份预览面板：id 块（OPERATOR/名字/状态）右侧**与 RrSuika Studio 齐平的 ACCESS FULL DOSSIER 链接**（24px `--text-scale-article-h3`，指向 about 页）；底部 meta 行（位置/领域）。无描述文字（2026-08-16 移除） |

**改 UI 的查找顺序：** 全局层（Navbar/Footer/ScrollMeter/背景）→ 组件文件；**projects/lab 列表（磁带盒书架）→ `CassetteShelf.astro`**；其余列表卡片（首页精选、notes）→ `ProjectCard.astro`；详情 → `ProjectDetail.astro`；首页区块 → `home/` 对应组件；文案 → `translations.ts`（含 `shelf` 段）；颜色/间距 → `global.css` token。**不要新写一套已有组件能做的事**（例：列表筛选已有 `.tag-bar`/`.tag-chip` 共享实现，§7.3）。

---

## 6. 样式与视觉设计系统

**唯一事实来源：`src/styles/global.css`（1070+ 行，14 个 section）。** 文件头含两条铁律：

1. **BREAKPOINT REGISTRY**：断点字面量清单 `1100 / 1000 / 900 / 800 / 768 / 769 / 700 / 600 / 500`（媒体查询里不能引用 CSS 变量，所以全局与组件里保持字面量，新增断点要与清单同步）。
2. **FIDELITY POLICY**：所有 token 值 1:1 复制自清理前原样式；**替换字面量时，仅当 token 在两主题值相同才允许**，否则保留字面量；**禁止发明新颜色、"harmonize"、重着色**。

### 6.1 主题契约

- `data-theme` 属性挂在 `<html>` 上（`"dark"` | `"light"`）；`localStorage["rrsuika-theme"]` 持久化；切换入口 `window.__toggleTheme`（Layout 脚本）+ Navbar 按钮。
- 切换的视觉是**圆圈波**（`src/scripts/theme-transition.ts` + `global.css §15`）：每次点击从点击处（无坐标则按钮中心）**叠一份整页拷贝**并用 `clip-path: circle()` 从半径 0 长到最远角，圆内是新主题的**真实像素**（不是一块填色——填色会把内容盖住，用户原话「画面黑屏，然后新界面突然从暗部里浮现」），连点即多层拷贝叠成黑白相间的水波；最后一份盖满视口时所有拷贝在同一帧撤掉、根落到目标主题，接缝为零。**不用 View Transitions**（同一时刻只能有一个 → 连点被丢），详见 v1.23 与决策 23。
- **首次访问默认 dark**（不跟随 `prefers-color-scheme`）；head 内联守卫脚本在任何绘制前设好 `data-theme`，配套 `html:not([data-theme]) body { visibility: hidden }` 防闪烁。
- 暗色 token 定义在 `:root`（§1），亮色覆盖在 `:root[data-theme="light"]`（§2，只覆盖值不同的 token；排版/布局/圆角/缓动 token 两主题同值只定义一次）。⚠️ **这条「深色没有属性专属块」是主题揭示的关键约束**：`:root` 选择器只匹配 `<html>`，所以嵌在文档里的拷贝必须靠运行时生成的 `.theme-scope…` 规则拿主题；而 `buildScope()` 必须**连裸 `:root` 一起复制**（深色 token 就在那里），否则深色拷贝只能继承活动根的颜色。同理，`data-theme` 在连点期间会被停到 `dark`（见决策 23），**任何在事件里读 `data-theme` 判断明暗的代码**（`GravitationalLens` 的 MutationObserver、about 页的 `isLight()`、Layout 星空 rAF）在这一小段里会看到 dark；星空那份每帧重读、自愈，透镜那份只是可能提前 init 一次。

### 6.2 核心设计 token（两主题值）

**颜色（Dark / Light）：**

| Token                                                                         | Dark                                                                                  | Light                                                                      |
| ----------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `--bg`                                                                        | `#07070d`                                                                             | `#f0ebe0`                                                                  |
| `--panel` / `--panel-light`                                                   | `#0e0e18` / `#161625`                                                                 | `#e5dfd3` / `#f8f4ec`                                                      |
| `--text` / `--text-bright` / `--text-soft` / `--text-tertiary` / `--text-dim` | `#eee4cf` / `#f6eedb` / `#e2d7bd` / `#b3a48a` / `#91846e`（2026-09 用户改暖，旧文档值已过时）                             | `#1a1815` / `#1a1a1a` / `#444444` / `#666666` / `#555555`                  |
| `--on-accent`                                                                 | `#000000`                                                                             | `#ffffff`                                                                  |
| `--border` / `--border-accent` / `--border-strong` / `--border-divider`       | `rgba(255,255,255,.10)` / `rgba(224,148,58,.25)` / `rgba(255,255,255,.2)` / `#333333` | `rgba(0,0,0,.08)` / `rgba(176,112,32,.25)` / `rgba(0,0,0,.15)` / `#cccccc` |
| `--accent`（真空管琥珀）/ `--accent-glow` / `--accent-deep`                   | `#e8943a` / `#f0b860` / `#b87020`                                                     | `#c07020` / `#d08030` / `#8b4513`                                          |
| `--stripe-red` / `--stripe-yellow` / `--stripe-teal`                          | `#e04040` / `#f0c040` / `#3ab8d8`                                                     | `#c03030` / `#b89020` / `#1a8090`                                          |
| `--terminal-green`（磷光绿）                                                  | `#3af04f`                                                                             | `#1a7030`                                                                  |
| `--retro-red` / `--retro-yellow` / `--retro-cyan`（复古标题色）               | `#d65a4a` / `#f6c85f` / `#5edfff`                                                     | `#b84a3a` / `#c49a2a` / `#3a9ebf`                                          |
| `--code-bg`                                                                   | `#161625`                                                                             | `#f5f5f5`                                                                  |
| 表面系列 `--surface-*`、`--paper`（两主题同值 `#e8e3da`）等                   | 见 global.css §1/§2                                                                   |                                                                            |

**字体：** `--font-mono: "JetBrains Mono", "PingFang SC", "Microsoft YaHei", "Noto Sans SC", "Courier New", monospace`；`--font-body: Arial, Helvetica, "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif`（JetBrains Mono 由 Layout 从 Google Fonts 加载 400/700/800/900）。

**字号**（`--text-scale-*`，暗色定义亮色复用）：hero `clamp(80px,10vw,110px)`、display `clamp(36px,5vw,64px)`、h1 64、h2 48、h3 34、article-h1 42、article-h2 32、article-h3 24、body 18、sm 15、xs 13 等。
**字距**（`--tracking-*`）：hero -6px、display -3px、tight -2px、slight -1px、micro 0.5px、label 1px、wider 1.5px、tag 2px、wide 3px。
**布局**：`--container-main: 1320px`、`--container-article: 1150px`、`--section-gap: 120px`、`--grid-gap: 30px` 等。
**圆角**：`--radius-xs 2px` … `--radius-lg 12px`、`--radius-cover 24px`、`--radius-pill 999px`。
**缓动**：`--ease-out`、`--ease-in-out`、弹簧族 `--spring-gentle/-snappy/-bounce/-expressive`、`--ease-warmup`（动画**时长**未 token 化，全部字面量）。
**阴影**：无通用 shadow token 族，仅亮色 `--shadow-paper`；其余阴影为组件内字面量。

### 6.3 背景层（§10）

**首页（en/zh/nl 三个 index）暗色模式叠层，由下到上固定为：纯黑底（`#blackhole-layer` 的 `background: #000`）→ 黑洞视频（`#blackhole-video`，`public/media/blackhole.mp4`）→ 星空（`#starfield-canvas`，桌面端暗色下由 `#lens-canvas` 引力透镜星空接管）→ 页面内容（`.site-content` z-index 1）+ 扫描线/暗角。** 三者同为 `z-index: 0`，靠 DOM 顺序决定前后（`#blackhole-layer` 在 `#starfield-canvas` 之前）；其他页面不渲染黑洞层，只有「`--bg` → 星空 → 内容」。黑洞视频层的尺寸/位置由 6 个 `--blackhole-*` 变量控制（定义在 global.css §10 的 `#blackhole-layer` 内，注释即说明书）；light 模式 `display: none`，`prefers-reduced-motion` 时脚本停播、只留首帧静帧；首帧解码完成后 0.5s 渐入（`.bh-fade` → `.is-ready`），避免加载时视频闪跳。**仅桌面端**：层默认 `display: none`，JS 在 `min-width: 769px` 时加 `.bh-on` 才显示，且 `<video>` 用 `data-src` 由 JS 挂载，手机端零请求（实测 390px 视口 `readyState=0`、`src` 为空）。

`#starfield-canvas` 动态星空画布（fixed，dark `opacity: 0.85`，light 隐藏星空，改用复古黑色四边形+十字网格背景，3D 天球式星星：真实恒星光谱色、自行运动、缓慢相机漂移 + 鼠标近旁星点局部拖拽动画、微光/十字星芒）+ hex grid（`body::before` 三向 linear-gradient，`52px 90px`）+ noise（`body::after` SVG feTurbulence data-URI）+ scanlines（`#crt-overlay`，dark `opacity: 0.28` / light `0.08`，`mix-blend-mode: screen`）+ amber vignette（`#tube-vignette`，`opacity: 0.6` / `0.4`）。星空脚本在 Layout.astro 内（参数区：恒星数 90–3000（前端视锥内约 88% 正向采样，桌面端最多 3000 / 移动端约 900）、`AUTO_ROTATION = 0.00032`、鼠标近旁星点局部拖拽（不随鼠标整体旋转）、`STAR_WEIGHTS` 按真实恒星颜色分布；性能折衷：光晕仅在亮星启用、DPR cap 1.5；星空不再整体跟随鼠标，改为局部星点拖拽）。原 `#neural-canvas` 神经网络粒子画布与 2D 版星空脚本被暂时禁用保留（`<html data-neural="enabled">` / `data-star-version="2d"` 可回退）。**性能（2026-09-09）**：每颗星的径向渐变在首帧算一次并缓存到 `star.cache`（`prepareStar()`），绘制时用 `ctx.translate()` 复用；此前是每帧每星 `createRadialGradient()` + 4 次 `addColorStop()` + 现拼 rgba 字符串，600 颗星实测约 3.0ms/帧，缓存后约 0.9ms/帧（headless Chrome 软件渲染基准），这也是首页视频偶发掉帧的主因。

### 6.4 其他全局约定

- **§13 CJK**：`html[lang="zh"] h1/h2/h3 { letter-spacing: 0; line-height: 1.2; }`，但 `.hero h1` 是拉丁文本，恢复 `tracking-hero`/`line-height: 0.9`。
- **§14 Hero 终端隐藏**：`.hero .terminal { display: none; }`;恢复方法就是删掉这条规则（注释写明"无需修改任何其他文件"）。
- **滚动条全站完全隐藏**（`scrollbar-width: none` + `::-webkit-scrollbar { display: none }`）;用户偏好的原始行为，不要"修复"。
- **旧调色板字面量**（刻意保留，勿重着色）：Navbar 警告条纹 `#ff5f1f`、品牌三色点 `#ff3333/#ffd400/#00b0ff`、Hero 终端 `rgba(255,95,31,…)`、亮色绿 `#00703c`（Footer/AboutPreview/LatestUpdates/about 页）、ScrollMeter 亮色深绿 `#145a28`、§11 里 art gallery 冻结样式（注释 "frozen: kept verbatim"）。
- **Hero 条形码与标题同色（2026-09-09 用户要求）**：`.barcode` 的条纹颜色走 `--barcode-ink`（暗色 = `--text`、亮色 = `--text-bright`），与 FUNCTIONAL 标题逐字同色；原来是写死的 `#fff`，在暖白标题旁边偏冷偏灰。以后改色请改这两个变量，不要写死 `#fff`。
- 通用模式：`.tag-chip`（pill 筛选按钮，§12 共享实现）、`.data-tag`（mono 大写数据标签）、`.skip-link`、hover 用 `--ease-out`、按下 `--spring-snappy`；入场动画 `pageIn`/`cardIn` + 各网格容器 nth-child stagger（§8）；`prefers-reduced-motion` 全局降级（§9，并隐藏粒子画布）。
- 页面局部样式：写在各自 `.astro` 的 `<style>` 内（自动作用域）；少数 `is:global`（如 ProjectDetail 的 article 排版）。**全局设计系统级 CSS 只应进 global.css。**
- ⚠️ **`<style is:global>` 里一律写裸选择器，不要写 `:global(...)`**：Astro 只在**作用域**样式块里剥离 `:global()`；`is:global` 块会被原样输出，而 `:global(.x)` 不是合法 CSS 选择器，浏览器**整条规则丢弃**（静默失效，dev 与 build 一样）。详见 §10。

---

## 7. 功能与交互架构

| 功能                               | 实现位置                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **导航 + 语言切换 + 主题切换**     | `Navbar.astro`。语言切换是**镜像链接**（服务端 `getLocalizedPath` 生成 href），不是 JS 切换；详情页上先查译文条目（`getCollection` + `typeToRoute` 反查）决定目标，无译文时 JS `preventDefault` 并弹 `#language-toast`（移出 nav 追加到 body 规避 stacking context）。主题按钮 `onclick="window.__toggleTheme?.()"`，函数定义在 Layout 脚本                                                                                                                                                                                                                                                                                                                                        |
| **首页精选**                       | `featuredKeys` **硬编码在三个首页文件的 frontmatter**（en/zh/nl，不在组件里）：`["body-armor-protective-vest", "zoem-bike-cargo-box", "light-diffusion-test-platform"]`（zoem 用 translationKey 而非文件夹名）。**列表顺序 = 卡片展示顺序**（2026-08-16 起用 `map`+`find` 按 curated 顺序取条目；此前 filter 按 collection 顺序，改列表顺序无效）。改精选=改这三个页面                                                                                                                                                                                                                                                                                                             |
| **SYS.LOG 最近活动**               | 首页 frontmatter：过滤 `type !== "art"`（art 无详情页，避免 404）→ 按日期倒序 → 取 3 条 → 传 `<LatestUpdates/>`（内部用 `buildEntryUrl`）                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **列表筛选（projects/lab/notes）** | 三个列表页各自的 `.tag-bar`/`.tag-chip`（样式在 global.css §12 共享）；页面 frontmatter 硬编码 `filterTags`（en/zh 各一套，内容不同）；客户端 JS 点击 chip 按 `data-tags` 显示/隐藏。projects/lab 用 ProjectCard 网格，notes 是自绘 `a.note-row` 列表（只显示前 3 个 tag，链接用 `getLocalizedPath("/note", lang)` + slug）                                                                                                                                                                                                                                                                                                                                                        |
| **详情页交互**                     | `ProjectDetail.astro` 脚本：点击图片全屏灯箱、`#process-scroll` 无缝滚动轨道（可拖拽/悬停暂停）、侧边 TOC（锚点自动补 id、剥数字编号前缀；note 取 h2 标题级别）。正文图片 src 在构建期由 `markdown-resolve-images.ts` 解析，无客户端修正                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **ScrollMeter**                    | `ScrollMeter.astro`：scroll 监听（rAF 节流）→ 10 段 LED + 百分比文本；回顶/回底平滑滚动；轨道 `pointer` 拖拽滚动；≤768px 隐藏；`prefers-reduced-motion` 关闭过渡                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **Hero 打字机**                    | `home/Hero.astro`：SYS.BOOT 启动序列 + 循环 live logs（终端整体被 global.css §14 隐藏）；左列文字逐字打字                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **art 页（冻结）**                 | `art/index.astro` + `zh/art/index.astro` 各自独立实现：`import.meta.glob(eager)` 扫描 4 个 art 条目文件夹图片（排除 `cover.png`）、硬编码 4 分类（CATEGORIES）+ 按语言作品名（TITLE_MAP）、每行无缝跑马灯（悬停暂停、拖拽 1.5×、点击 lightbox、Esc/←/→/触摸滑动）。**两语言文件实现有差异**（en 用 opacity 动画，zh 用 display 切换）                                                                                                                                                                                                                                                                                                                                              |
| **about 页**                       | 三个 about 页（en/zh/nl，独立文件）：design-process 站台自动轮播（IntersectionObserver 激活，1.5s/步）、capability matrix canvas（8 个绘制函数：3D 环面/CMF 色块/示波器/波纹/DNA 螺旋/矿石传送带/像素网格/软件终端，4.2s 轮播，`define:vars` 注入翻译）、**技能星图**（2026-09-01 重做，用户要求 Obsidian 关系图谱式效果：lieflat-charts B2 big-force 语法，ECharts 力导向网络——11 个技能大点（灰阶，亮度随连接数）+ 技能小点绕行 + 大点间骨干连线，亮度随连接数递增，定时轻微漂移，拖拽回弹、hover 邻接聚焦、点击空白重播、缩放平移；echarts@6 CDN `is:inline defer` 加载 + 轮询 boot；数据标签经 `t.skillMap` 三语注入，坐标/连线/大小在页面脚本）、ANNEX 打字机彩蛋。**dossier 卡**（PERSONNEL_DOSSIER // ABOUT_MODULE）：左列 identity（OPERATOR/名字/角色 + LOCATION/FIELD/INTERESTS mini 行;2026-08-16 平衡左右列移到左侧），右列 data 行（CURRENT_FOCUS/SKILLS 芯片/SOFTWARE 图标）。软件图标在 `public/icons/`（6 个灰度 logo），按明暗极性打 `pol-dark`/`pol-light` 类：暗主题 `pol-dark` 加 `invert(0.9)`，亮主题 `pol-light` 加 `brightness(0.3)`（2026-08-16 新增） |
| **背景星空**                       | `#starfield-canvas` 3D 天球星空（Layout.astro 脚本，参数见 §6.3）；原神经网络 `#neural-canvas` 被暂时禁用保留，`prefers-reduced-motion` 时隐藏                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **移动端提示**                     | `#mobile-notice`：sessionStorage `"rrsuika-mobile-notice"` 记忆关闭；桌面（≥769px）隐藏                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| **结构化数据**                     | Layout head：`WebSite`（每页）；about 页：`Person`（name "RrSuika Studio"、alternateName `["RrS"]`、sameAs GitHub `RrSuika` + pixiv、Rotterdam NL）；详情模板：`TechArticle`（含 `translationOfWork`，§3.2-6）                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

---

## 8. 配置与部署

- **`astro.config.mjs`**（约 25 行）：`site: "https://rrsuika-studio.pages.dev"`；`trailingSlash: "always"`（全站 URL 尾斜杠约定，§9 决策 16）；`markdown.processor: satteri({ hastPlugins: [resolveEntryImages] })`（正文图片构建时解析，§9 决策 17，`satteri` 来自 `@astrojs/markdown-satteri`——Astro 7 默认 Markdown 处理器，已显式写入 dependencies）；`i18n: { locales: ["en","zh","nl"], defaultLocale: "en", routing: { prefixDefaultLocale: false } }`（**声明而已;实际路由全部手写**，Astro i18n routing 未被使用）；无 integrations、无 compressHTML 覆盖、无 redirects。
- **`package.json`**：scripts `dev`/`build`/`preview`/`astro`（均标准 Astro）；dependencies 仅 `astro ^7.1.6`、`zod ^4.4.3`；无 devDependencies；`engines: node >= 22.12.0`。
- **`tsconfig.json`**：`extends "astro/tsconfigs/strict"`，exclude `dist`。
- **开发**：`npm run dev`（localhost:4321）或根目录 `启动.bat`；按 AGENTS.md 约定，AI 应用 `astro dev --background` 启动、`astro dev stop/status/logs` 管理。
- **构建**：`npm run build` → 输出 `dist/`（纯静态，无 SSR/适配器）。
- **部署**：Cloudflare Pages Git 集成，push `main` 自动构建部署；**仓库内无 CI 配置**（无 `.github/workflows`、无 `wrangler.toml`、无 `_headers`/`_redirects`），构建命令/输出目录配置在 Cloudflare 控制台。
- **环境变量**：**无**。项目不需要任何 .env（.gitignore 含 `.env`/`.env.production` 仅防御性条目）。
- **SEO 静态文件**：`public/robots.txt`（`User-agent: *` 全允许；11 个 AI 训练爬虫 GPTBot/ChatGPT-User/ClaudeBot/Claude-Web/anthropic-ai/Google-Extended/CCBot/PerplexityBot/Meta-ExternalAgent/cohere-ai/Bytespider 被 `Disallow: /art/` 和 `/zh/art/`；Googlebot 不受影响；Sitemap 指向站点根）；`public/google0d8…b1.html` 是 Search Console 验证文件，勿删。
- **OG 分享卡**：`public/og-card.png`（1200×630）为全站默认 `og:image` 回退（Layout 逻辑：`ogImage ?? "/og-card.png"`）；修改需重跑 `node scripts/og-card-gen.mjs`（内联 SVG 硬编码→sharp 渲染）。**注意**：脚本 `import sharp` 依赖的是 astro 的传递依赖（未在 package.json 声明、无 npm script）;脆弱点见 §10。

---

## 9. 重要设计决策（"为什么现在是这样"）

1. **Astro + 零集成**：站点以静态内容为主、无重交互，追求构建简单与极致静态性能；不需要框架岛屿。所有交互用少量原生 JS 内联实现。
2. **单一 Content Collection（entries）+ 多 `type` 字段**：全部内容共用一份 schema 和一套动态路由，新增条目零代码。`type` 枚举 `projects/lab/note/art` 按内容性质分类（作品集/实验记录/学习笔记/艺术），**不是** `work`;不要按老文档或直觉改名。`gallery` 是字段不是 type。
3. **手动 i18n / 每语言独立页面文件**：en 与 zh 页面是**两套独立文件**而非共享模板，因为 about/art 页两语言差异极大（脚本、动画参数、样式覆盖都不同）。代价是双维护;这是被接受的设计，不是缺陷（新静态页必须建 zh 镜像）。
4. **双动态路由分工**（`[type]/[...slug]` 管 en、`[lang]/[type]/[slug]` 管 zh 且 lang 硬编码 "zh"）：让 en 保持无前缀、zh 有 `/zh` 前缀，两个文件对称可读。`[...slug]` rest 是前瞻设计（当前只用到单段）。
5. **`/note/{slug}` 单数 vs `/notes` 列表**：历史上出现过分叉的映射生成 `/notes/{slug}` 产生 404；此后 `routes.ts` 被定为链接生成的单一事实来源（见 §10 的残留偏差）。
6. **首页精选用硬编码 `featuredKeys`**：用户偏好人工策展（"featured" 字段反而只作声明）；改精选要动两个首页文件的前端代码。
7. **SYS.LOG 排除 art**：art 无详情页，排除避免首页链接到 404;任何"最新内容"类列表都应遵守此过滤。
8. **设计系统清理时的 FIDELITY POLICY**：token 值 1:1 复制原样式，宁可保留字面量也不"统一"颜色；旧调色板（`#ff5f1f` 警告橙、三原色点、`#00703c`/`#145a28` 旧绿）是用户选择保留的原始视觉，**不要重着色**（CLAUDE.md 同款规则）。
9. **Hero 终端临时隐藏（§14）**：用户评估无终端首页是否更清爽的实验；元素完好，恢复=删一条 CSS 规则。
10. **art 页面冻结**：用户决定不再改动 art 列表页、ArtGallery、gallery schema 字段（只允许改 SEO frontmatter）。相关代码视为历史冻结区。
11. **滚动条完全隐藏**：原始设计保留的用户偏好（牺牲可发现性，用户知情）。
12. **主题默认暗色**：不跟随系统偏好，首次访问一律 dark（防闪烁守卫 + localStorage）。
13. **fashion-design 图片双存**：`public/art/fashion-design/` 的 PNG 副本是为保留 alpha 透明通道（`getProjectImage` 特例分支），删除会破坏 art 页透明效果。
14. **全站 JSON-LD**：WebSite（每页）+ Person（about）+ TechArticle（详情）为 SEO 富结果服务；日期必须完整 ISO-8601 带时区（schema.org 要求）。
15. **og-card.png 手工生成**：分享卡视觉（终端风格 + 三色条 + 中英标语）由脚本硬编码 SVG 渲染，无构建步骤;改分享卡要改脚本并手动重跑。
16. **全站 URL 统一尾斜杠（2026-08-31）**：`astro.config.mjs` 设 `trailingSlash: "always"`（canonical/hreflang/og:url 自动带尾斜杠），`getLocalizedPath`/`buildEntryUrl`/`sitemap.xml.ts` 均输出尾斜杠，硬编码链接（404 页按钮、JSON-LD `url`）也带尾斜杠。原因：Cloudflare Pages 对目录型页面把不带斜杠的请求 308 重定向到带斜杠版本；默认 `"ignore"` 会让每个页面的 canonical 指向重定向 URL，Google Search Console 报"网页会自动重定向"并拖累收录。**新增内部链接必须带尾斜杠**（唯一例外：资源文件与首页 `/`）。
17. **正文图片构建时解析（2026-08-31）**：新增 `src/utils/markdown-resolve-images.ts`（Sätteri raw 节点插件，经 `markdown.processor: satteri({ hastPlugins })` 注册），把正文原始 HTML 中 `<img src="./…">` 重写为 `__ASTRO_IMAGE_` 标记并加入 `localImagePaths`，由 Astro 原生内容图片管线（image-marker + `updateImageReferencesInBody`）在渲染期解析为哈希 webp URL；同时删除了 ProjectDetail 里旧的客户端图片修正（隐藏 div 图片表 + DOMContentLoaded 重写）。原因：浏览器解析 HTML 时先用相对路径发请求，Cloudflare Error Monitoring 报大量详情页图片 404；markdown 语法图片（`![](./x.png)`）本就走原生管线，只缺原始 HTML 图片。**关键坑**：① 插件随 astro.config 打包，配置 bundle 里 `import.meta.glob(eager)` 的图片值没有 astro:assets 处理（`.src` 是 `/src/content/...` 原始路径，生产 404），插件内只能使用 lazy glob 的**键**做存在性检查，绝不解析**值**；② Astro 内容层把渲染结果缓存于 `node_modules/.astro/data-store.json`（仅按文件摘要失效），改动 markdown 管线配置后须删除 `node_modules/.astro` 强制重渲（Cloudflare 从干净环境构建，部署不受影响）。
18. **首页黑洞视频背景层（2026-09-09）**：用户要求首页暗色模式下叠层为「纯黑色背景 → 黑洞视频 → 星空 → 其他元素」，其他页面保持原样。实现方式：视频原样拷贝为 `public/media/blackhole.mp4`（1920×1080/60fps/约 5s/3.1MB，h264 yuv420p，**未转码**），`Layout` 新增 `blackhole` 布尔 prop，为 true 时在 `#starfield-canvas` **之前**插入 `#blackhole-layer`（`position: fixed; inset: 0; z-index: 0; background: #000; overflow: hidden`）内含 `<video autoplay muted loop playsinline preload="auto">`；三者同为 `z-index: 0`，因此**层序完全由 DOM 顺序决定**——若日后移动标记位置，层序会跟着变。只有三个首页传 `blackhole`。light 模式整层 `display: none`（保留复古网格）；`prefers-reduced-motion` 时脚本 `pause()` 只留首帧（不删除层，视觉构图不塌）。**渐入**：JS 立刻给层加 `.bh-fade`（`opacity: 0` + 0.35s 过渡），在视频 `loadeddata`/`canplay` 时加 `.is-ready` 淡入，避免首帧解码期间「闪一下」；无 JS 时 `#blackhole-layer:not(.bh-fade)` 的 `blackholeIn` 动画兜底，另有 1.5s 超时保护防止永久不可见。渐入时长在 global.css §10 的 `#blackhole-layer.bh-fade` 一处改。尺寸/位置用 `--blackhole-*` 自定义属性（x/y/width/height/scale/opacity，定义在 `#blackhole-layer` 内）+ `object-fit` 调节，注释写在 global.css §10。**`--blackhole-y` 线性跟随视口高度**：`calc(122px + 37.8vh)`。用用户现场 HUD 调出的两组真实数据拟合——视口高 562px（1080p 屏）时 59.5% = 334px，视口高 802px（2K 屏）时 53.0% = 425px（两点都在 0.1px 内复现）。**教训**：纯百分比（52%/60%）或按"屏幕分辨率"的假设都靠不住——用户两块屏的实际视口只有 562/802px（Windows 缩放 + 窗口非最大化），百分比会随窗口高度漂移；`clamp(52%, calc(345.6px + 28vh), 60%)` 和 `@media (min-height: 1200px)` 两档方案都因为假设视口≈屏幕分辨率而失效。以后调这个值，用 Layout 里那个 `?bhtune=1` 工具量真实数据。**桌面端限定**：`<video>` 不带 `autoplay`/`src`，改用 `data-src`，脚本只在 `min-width: 769px` 时挂载并播放，手机端既不显示也不下载（CSS 也加了 `@media (min-width: 769px)` 双保险）。**权衡**：`#blackhole-layer` 的纯黑底会盖住 `body::before` 的 hex grid（首页暗色模式下网格不可见），这是层序要求的直接结果；`body::after` 噪点与 `#crt-overlay`/`#tube-vignette` 仍在最上层，保持整机 CRT 质感。
19. **星空每帧开销优化（2026-09-09，配合首页视频）**：`drawStar()` 原先每帧为每颗星新建径向渐变并拼接 4 个 rgba 字符串（500–1200 颗 × 60fps），实测 600 颗星约 3.0ms/帧，抢占主线程导致首页黑洞视频偶发掉帧。改为 `prepareStar()` 在首帧算好 `glow`（原点渐变）、`glowRadius`、`coreRadius`、`flare`、`alphaBase` 缓存到 `star.cache`，绘制时 `ctx.save(); ctx.translate(sx, sy)` 复用同一条渐变，alpha 改由 `globalAlpha` 承担（数学上与旧版逐 stop 乘 alpha 等价），实测降到约 0.9ms/帧，渲染结果不变。**注意**：缓存假设相机固定（`camYaw`/`camPitch` 恒为 0）；若日后恢复相机旋转，必须改为每帧重算或按帧失效。同批还加了：`#blackhole-video { will-change: transform }`（独占合成层）、**视频持续播放**——原先「标签页隐藏 / 切亮色主题就暂停」的省解码逻辑已被用户要求去掉（会引起各种奇怪的停播），改成 2s 看门狗兜底重播（仅桌面端）。
20. **引力透镜星空（2026-09-10）**：首页暗色 + 桌面端（≥769px）在黑洞视频之上叠加 `#lens-canvas`（`src/components/GravitationalLens.astro`），并接管原 `#starfield-canvas`（`html[data-lens="on"]` 隐藏旧星空，Layout 脚本检测到该属性即跳过绘制，避免双份 canvas 开销）。**速度分段 + 单股连续内流（2026-09-10 重构）**：`rMid ≤ r < rOuter` 外层段（`--lens-speed-outer` 极慢）、`rInner ≤ r < rMid` 中层段（`--lens-speed-mid` 快）、`rRing ≤ r < rInner` 内层段（速度按 `--lens-inner-ease` 从 `speedMid` 缓降到 20%），`r < rRing` **完全不绘制**——视频黑洞的纯黑透出，圈内没有任何星星（用户明确要求）。用户要求星**跨层连续**，原「三层各自循环、层内边界回收」是错误行为，已弃——同一颗星自外场边缘出生后沿半径**单调内落**、穿越全部速度分段，途中被吸积盘遮住时继续下落（`diskMaskAlpha` 只隐藏绘制、不打断运动），离开盘内缘后重新露出，直到黑洞圈前淡出（`--lens-r-ring` 处 alpha 已为 0），`s.r ≤ rRing` 才在圈上回收重生。重生位置**不均匀**：稳态密度 ∝ 1/速度（`speedAt` 定义的速度曲线各处自洽），`buildSpawn()` 用数值 CDF——在 `[rRing+fRing, rOuter]` 上 512 步积分 `dr/speed`（fRing = 圈前淡出带宽，不在淡出尾内播种）+ 二分采样——初始帧即稳态密度、无松弛波、无边界缝。`rMid` 两侧速度用**平滑斜坡**过渡（smoothstep，半宽 `rampW` = min(两段跨度)×约 12%，clamp 12–80px）取代速度跳变线；内层段在 `rInner` 处取值恰为 `speedMid`（连续）。**旋进**：内流同时按 `--lens-swirl`（∝ `rRing/r`）顺时针螺旋落入；**吸积盘遮罩**：旋转+压扁的椭圆环（`--lens-disk-inner-w/h`、`--lens-disk-outer-w/h`、`--lens-disk-tilt/dx/dy/feather`，整体上移使下半更小）内的星（按透镜后屏幕半径）完全不画，进入时按 `feather` 淡出、从 `diskInner` 内边界淡入（不再硬切）。**光子带**：黑洞圈外那圈星是**圆形**的（不做切向拉伸，`--lens-stretch` 对它们无效），更小（`--lens-photon-size`，光晕同步缩小）也更亮（`--lens-gap-boost`），宽度 `--lens-photon-band`（×视口高）填满「盘内边→黑洞圈」的窄缝；宽度应小于盘内边界的最小半轴（`--lens-disk-inner-h` − `--lens-r-ring`，默认 0.276−0.25≈0.026），否则圆带会被椭圆遮罩裁成椭圆。**引力透镜近似**：靠近 `rRing` 时 `--lens-bulge` 向外推、`--lens-stretch` 沿切向拉长成弧（爱因斯坦环感），同时提亮再淡出，所以星看起来「到黑洞外圈就消失」。**调参工具**：点导航栏 SYS_STATUS 左侧的齿轮图标（绝对定位、不占流，所以中间导航不会位移；颜色同导航链接 `#aaa`）、按反引号或 `?lens=tune` 打开磨砂玻璃 HUD（拖标题栏可移动，位置存本机（拖动限制在窗口内，不会拖丢）；滚动条为细窄主题样式；底部实时显示当前选项说明，文案随页面语言 en/zh/nl 切换（来自 `translations.ts` 的 `lensTune`））；`1`=CENTRE、`2`–`5`=外/中/内/黑洞外圈半径、`6`–`0`=吸积盘移动（←→x / ↑↓y）、内外边界（←→宽 / ↑↓高）、倾角、羽化，其余用 `TAB` 循环；方向键调（双轴目标 ←→ 调第一轴、↑↓ 调第二轴；单值目标 ←→/↑↓ 都调它），`[`/`]` 调步长、`Shift` ×10、`Alt` ×0.1、`H` 开关参考圈、点 **COPY 按钮**（或按 `C`）复制完整的 `#lens-canvas { … }`（含视口与 px），再 `R` 复位。改动实时写入 `#lens-canvas` inline 自定义属性并持久化到 `localStorage["rrsuika-lens-tune-v7"]`——**只存被 nudge 过的旋钮增量**（v6 存全量状态，会把某屏调出的数字冻结到所有屏；默认值已视口自适应，见决策 21），只存本机、不进仓库、不影响访客。**默认值视口自适应（2026-09-10）**：`--lens-r-ring` 默认 0.25（1661×802 实测烘焙 201px）与 `--lens-cy-vh` 0.436 等 7 个视频锚定旋钮不再是"每个屏都要手调"的单点值，而是自适应模型的两个校准锚之一——**换屏幕/拖窗口到别的显示器会自动重新对齐**，见决策 21。
21. **引力透镜视口自适应默认值（2026-09-10）**：用户反馈——§10 同一套参数在 2K 屏（视口 1661×802）调好后，把窗口拖到 1080p 屏（视口 1234×562），黑洞圈与中心对不上视频；随后给出他在 1080p 屏手调出的整套最佳参数作校准数据。两档数据严格符合同一缩放律：视频盒 125% + `object-fit: cover`，凡宽高比 ≥16:9（width-bound）的窗口，视频所有特征随**视口宽度 W** 缩放（两组实测互证：rRing px = 200.5@1661 与 149@1234 ≈ 0.1207·W；中心 y = 122 + 0.378·H + δ(W)，δ ≈ 0.028·W）。**实现**（`GravitationalLens.astro` §2a）：7 个视频锚定旋钮——`--lens-cy-vh`、`--lens-r-outer`、`--lens-r-mid`、`--lens-r-inner`、`--lens-r-ring`、`--lens-disk-inner-w/h`——逐特征做两点线性拟合 px = m·W + b（锚 A = §10/FALLBACK 发货基线 = 2K 档；锚 B = 用户 1080p 档；`SCALED` 表内 `a` 引用 FALLBACK 自动跟随），在当前视口下 ÷H 还原成无单位值。`cyVh` 特殊处理：cyPx 两档同为 122（真锚点，不进拟合），`cyVh` 编码的是「视频盒中心线 0.378·H + 随视频宽度走的屏幕偏移 δ」，先拟合 δ 再还原，避免把 H 掺进 W 拟合。**优先级（按来源、不按数值）**：tuner inline 覆盖永远逐字生效（nudge 回基线数值也算显式覆盖）；≠FALLBACK 的手改 §10 块逐字生效（显式冻结）；只有 CSS 恰好等于发货基线才做视口拟合。宽高比 <16:9（height-bound）或 W/H 未定（初始化时序：wire-up 的 readCfg 先于 measure）时保持原基线。**重刷时机**：`measure()` 量完 W/H 后重读 `readCfg()`（启动与 resize 都走它——把窗口拖到另一块屏即自动重拟合，无需刷新）。**HUD/COPY/nudge 一致**：`currentVal` 全部改走 `effectiveVal`（同一拟合路径），面板数值即实际渲染值；COPY 输出若含自动拟合值会加 ⚠ 注释——粘贴的块是**静态快照**，重新基线化必须 global.css §10 与组件内 FALLBACK/SCALED **两处同步改**。**经验**：视频锚定特征按视口宽缩放、且只有两点标定，跨屏只能靠线性外推（与决策 18 `--blackhole-y` 的教训同源：永远不要假设视口等于屏幕分辨率）。

22. **`backdrop-filter` 前缀对会被构建期压缩器合并（2026-09-10）**：用户报「localhost 卡片有毛玻璃，生产站没有」。先排除部署问题——生产 HTML 引用的 CSS 与本地 `dist` **逐字节一致**（同名 hash 文件，5148 bytes），所以不是缓存或部署滞后。真因在压缩器：Vite 8 用 **lightningcss**（`npm ls lightningcss` 可见，astro@7.1.6 的传递依赖；项目无 browserslist 字段）压缩 CSS，它把 `-webkit-backdrop-filter` 当成 `backdrop-filter` 的**别名**，同一条规则里成对出现就合并、**保留最后一条**。`ProjectCard.astro` 原本先写无前缀、后写 `-webkit-` → 产物只剩 `-webkit-backdrop-filter:blur(20px)saturate(160%)`。而 **Chrome/Edge/Firefox 根本不认带前缀的那个**（用 headless Chrome 152 做了对照渲染：只有无前缀的方块背景糊掉，只有前缀的方块和空白对照一样清晰），只有 Safari ≤17 认——于是生产站（Blink 系浏览器）完全没有毛玻璃，dev server 不做压缩、两条声明都在，本地一切正常。**修法**：无前缀那条留在 `.card` 规则里，Safari 的 `-webkit-` 兜底挪到紧跟 `.card` 之后的独立 `@supports (-webkit-backdrop-filter: blur(1px)) { .card { … } }`——一条规则里只有一个声明，压缩器无从合并；已验证产物里两条并存、`@supports` 包装保留（`@supports ((-webkit-backdrop-filter:blur(1px))){…}`）。**教训**：① 「dev 正常、生产失效」的纯 CSS 问题，第一嫌疑是压缩器而不是部署——先比对生产与本地 `dist` 的产物字节；② 任何「前缀 + 无前缀」成对的属性（`backdrop-filter`、`mask`、`user-select`、`text-size-adjust` …）都别写进同一条规则；③ 查这类问题时别用 `grep -c`（压低后的 CSS 只有一行，计数恒为 1，会误判），要用 `grep -o … | wc -l` 或正则 `Matches().Count` 数出现次数。`GravitationalLens.astro` 调参 HUD 里的同款成对写法躺在一个 JS 模板字符串里、不经压缩器，暂时安全——若哪天把它挪进 `<style>`，必须按同样方式拆开。

22b. **projects / lab 从卡片网格改成磁带盒书架（2026-09-18）**：用户要求把 `projects` 与 `lab` 的卡片重做成**磁带盒**，封面沿用原卡片图，并模仿《绝区零》「代理人秘闻」的书架＋书本切换动画（黑幕渐入 → 从下往上左→右依次升架 → 中心那本被抽出 → 转 15° 同时露封面/左侧/底部，其余后退并向两边倾斜成倒三角 → 右侧显示简介 → 点击进详情页；拖动＝放回书架，超过阈值换下一本）。用户另给两份 three.js 参考（`输入/磁带盒启动动画.html`、`输入/磁带盒主体动画.html`）并说明「启动动画只作动画参考，它的磁带盒视觉不如主体动画」。**决策：用 CSS 3D 而不是移植 three.js**（用户把技术选型交给我，只要求「最好看的视觉特效 + 兼顾性能」，并允许手机端退化成平面卡片）。三条理由：① 移植要新增 three 依赖（~600KB）进一个「无任何集成」的纯静态站；② three.js 版本里所有文案都得画进 canvas 贴图 → 三语、SEO、无障碍全丢；③ 站点的主题 token／毛玻璃／CRT 观感无法复用。姿态数学则是**逐行移植**：`layoutPose()` 的世界单位（W 4.85 / H 3.10 / 视野高 6.545 / 相机 11.8）全部保留，CSS 侧 `--S` = 每世界单位多少 px、`perspective: calc(var(--S) * 11.8)`，`WORLD_H` 收到 **5.6**（参考是 6.55）让书架占满舞台；外壳深度从参考的 0.46 加到 **0.70**（否则 15° 下左侧壁投影只有 ~11px，读不出厚度）。**版面预算（用户「太小了，底部可以贴着 footer 的边缘」）**：页高 = `100svh − 76px`、**去掉底部 padding**、并把 footer 的 `--section-gap`（120px）在这六页清零（`!important`，因为 Footer.astro 是 scoped 的 0,2,0 选择器）——否则书架底与 footer 之间会留一条 120px 空带；页头压成**一行**（左标题 / 右筛选 chips），段落简介移进舞台顶部那片本来就空的区域；`.shelf-track` 加 `transform: translateY(var(--shelf-drop, 13%))` 把整个场景压低，让书架底沿贴着 footer 上沿（track 级 2D translate 安全：它是 3D 上下文的根子元素，不会把 preserve-3d 压平）。结果：1600×900 下舞台从 639px → **753px**、`--S` 101→**134.5**、主角磁带 **733×502px**（原 539×345，+36%/+45%），底部留 31px 给接触阴影。**想再大只能继续压页头**——再高就会让磁带超出首屏下沿。**符号约定**（最容易错的一步）：Three.js 的欧拉角 `(rx,ry,rz)` 到 CSS 是 `translate3d(x, −y, z) rotateX(−rx) rotateY(ry) rotateZ(−rz)`——CSS 的 Y 轴朝下、rotateX/rotateZ 反向；**实测确认参考的 `ry = −15°` 露的是「右」侧壁**（用 `elementFromPoint` 逐点扫描渲染图验证），而用户要的是「左侧」，所以这里取 **`+15°`**；升架时的侧面朝向也相应改成「slot ≥ 0 → +90°」。**preserve-3d 卫生是本组件唯一的崩溃点**：`.cas-f` 面的任何祖先只要沾上 `overflow:hidden`／`opacity < 1`／`filter`／`backdrop-filter`／`clip-path`／`mask`／`mix-blend-mode`／非 3D `transform`，`transform-style` 就被强制降为 `flat`，整盒塌成平面卡（本次就是把逐盘淡入改走 `--cas-o` 变量、由**面**消费才修好）。**验证方法值得复用**：headless Chrome 用 `--virtual-time-budget` 会饿死 `requestAnimationFrame`，所有 rAF 驱动的 transform 都拍不到——改成 CDP（`--remote-debugging-port` + 页面内 `Runtime.evaluate`）走真实时间，并在页面里用 `document.elementsFromPoint` 打网格扫描出「哪张面画在哪」的 ASCII 渲染图，比肉眼看截图更准。手机端：**≤860px / `prefers-reduced-motion` / 不支持 `preserve-3d` ⇒ `data-mode="flat"`**，同一套 markUp 与状态机、纯平面卡片切换（模式标记挂在**页面根** `.shelf-page` 上，因为 flat 模式要改页面外壳的布局）。完整开场**每会话只播一次**（`sessionStorage`），REPLAY INTRO 与标签切换重播。六页共用同一个组件，`ProjectCard` 只剩首页精选在用。见 §5.2、§11。
23. **主题切换是「整页拷贝的圆圈波」，不是填色圆（2026-09-18 v1.23 定稿，取代 v1.19–v1.20 的填色方案）**：用户先要「去掉闪一下 + 频繁点击产生黑白水波纹」，v1.19/v1.20 用「填充目标主题色的圆」做到后发现**这个方案本身是错的**——圆里是一块纯色，会把所有文字和卡片盖住，用户原话「圆会覆盖整个画面，画面黑屏，然后新界面突然从暗部里浮现…圆和之后的浮现很割裂」。定稿改成：每次点击往 `.theme-stack` 叠一份**整页拷贝**（`clip-path` 从半径 0 长到最远角），圆里是**新主题的真实像素**，最后一份盖满视口时所有拷贝在同一帧撤掉，视觉上什么都不变。实现 `src/scripts/theme-transition.ts` + `global.css §15`（入口 `toggleThemeWithReveal(origin?)`，由 `Navbar` 的 `window.__toggleTheme(event)` 转发指针坐标）。**六条不变量，每条都有实测依据**：① **不能用 View Transition 拿快照**（虽然它给的正是真像素）：同一时刻只能有一个 transition，揭示期间的点击会被丢掉、按钮在动画期间是死的——用户报的就是这个；拷贝没有这个限制，N 次点击就是 N 个同时生长的圆、主题交替 ✓。② **拷贝要一个「根不是它」的主题**：项目里所有浅色规则都是 `:root[data-theme="light"] …`，而 `:root` 只匹配 `<html>`、和拷贝挂在谁下面无关，于是**深色拷贝会被当前浅色根的主题规则命中**（实测：深色拷贝里的书架面板变成 `rgb(206,203,198)` 的浅色，全帧平均 |Δ| **107**）。两处一起解决：`buildScope()` 在运行时把**所有以 `:root` 开头的规则**（**包括裸 `:root` 那一块——深色 token 就在那里，项目里根本没有 `:root[data-theme="dark"]`**）改写成 `.theme-scope…` 塞进一张 `<style>`，拷贝带这个 class；同时一轮连点期间把根**停在 `NEUTRAL_THEME`（dark，唯一没有属性专属规则的主题）**，连点结束后再落到目标主题。修好后同一对照是 |Δ| **1.0–2.0**（页面实测 1.95，面板+主角区 2.04）。③ **落地那一帧必须关掉过渡**：`body` 自己就有 400ms 的 background/color 过渡，拷贝撤掉之后再跑这段过渡，就是「从旧主题淡入新主题」——又变回用户骂的那个「浮现」。`applyInstant()` 给 `html` 加 `.theme-land`（`transition: none !important`）→ 换主题 → 强刷一次 recalc → 摘 class。实测落地帧 `body` 背景已是 `rgb(240,235,224)`、在飞的过渡 **0** 个、与纯浅色渲染的 |Δ| **0.09**。④ **不要在拷贝里 pause 动画**：拷贝是插入时才启动动画的，`animation-play-state: paused` 会把它冻在**第 0 帧**（入场动画的「之前」状态——书架磁带整片透明，实测 |Δ| **37**），`animation-delay: -3600s` 更糟（**106**，正好落在某个无限光晕最亮的一帧）；什么都不动才是对的（**2.8**，只剩几个 idle 动画的相位差），书架入场是 JS 驱动的、拷贝里没有可重播的入场。⑤ **拷贝不是 backdrop root**：拷贝里 `.shelf-panel`/`.card` 的 `backdrop-filter` 会去采样**拷贝后面**的页面，所以 `.theme-ripple` 自己带一个 `backdrop-filter: blur(0px)`（不画任何东西，只做隔离）。⑥ **拷贝要手工补齐克隆补不上的东西**：`canvas` 克隆是空的（按序号 `drawImage` 拷位图）、`video` 克隆会**再开一个解码器**（把当前帧画进 `poster`，一轮连点只做一次、缓存）、静态内容要跟着滚动偏移（`margin-top: -scrollY`，滚动时同步）、**与停放主题相同的拷贝直接不做**（活动文档本身就是那一层，省掉整轮最贵的一次）。**验证方法（本轮全靠它定案）**：解码截图逐像素比对——(a) 拷贝 vs 同主题的实时渲染（|Δ| ≤2 才算「没有肉眼可见差别」，顺带证明 **clone 里的 3D 书架没有被 `clip-path` 压平**）；(b) 揭示中途圆内 vs 新主题、圆外 vs 旧主题（实测圆内 |Δ| 1.26 / 对面 204）；(c) 在画面上撒 11×7 个采样点，每个点都必须落在「旧主题」或「新主题」之一上（77/77，证明**任何一帧都不是一块纯色**）；(d) 轮询到拷贝清空的那一刻读 `getComputedStyle(body).backgroundColor` 与 `document.getAnimations()`（抓落地帧）。⚠️ 测这些**永远不要 `a.cancel()`** 圆的动画：burst 靠 `finished` 收尾，取消会让它卡住（1.6s 的兜底计时器才会救回来）。**已知代价**：插入一份拷贝约等于一个长帧（`cloneNode`+插入本身只要 1.3–3ms，长的是浏览器给 ~500 个节点做 layout+paint：headless ANGLE 下最差帧 50–120ms，而同机 idle 帧本来就要 33ms），平均帧时间不受影响（+1ms），所以单次切换是顺的、快速连点每次点击各付一个顿帧。手机/`prefers-reduced-motion`/scope 构建失败一律退回瞬切。详见 v1.23、§6.1、§11。

---

## 10. 已知问题与技术债

状态标记：🟢 已解决 ｜ 🟡 当前存在 ｜ 🔵 暂不处理（有意为之） ｜ 🔴 未来计划

| 状态 | 问题                                   | 说明 / 注意                                                                                                                                                                                           |
| ---- | -------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 🟢   | ProjectCard 自建 routeMap              | 已修复（2026-08-14）：改用 `buildEntryUrl(project, project.data.lang)`，行为等价，与 `typeToRoute` 单一来源对齐。                                                                                     |
| 🟡   | Hero 终端被隐藏（实验状态）            | global.css §14。用户尚未决定保留还是恢复。改首页 Hero 视觉时勿被"终端不见了"误导。                                                                                                                    |
| 🟡   | 神经网络背景被星空短暂替换（实验状态） | `#starfield-canvas` 3D 星空在 Layout.astro 激活；原 `#neural-canvas` 脚本被禁用保留，回退方式：`<html data-neural="enabled">`（神经网络）/ `data-star-version="2d"`（早期 2D 星空）。用户尚未决定是否删除神经网络。 |
| 🟢   | 首页黑洞背景（已实现，视频版）        | 2026-09-09 完成：首页暗色模式用 `输入/黑洞动画 - AI 插帧.mp4`（拷贝为 `public/media/blackhole.mp4`）作最底层视频，叠层「纯黑 → 视频 → 星空 → 内容」，见 §9 决策 18。`输入/Shadertoy - Kerr Newman Black Hole` 的实时着色器版本**仍未移植**，作为可选升级保留；若日后移植，只需把 `#blackhole-video` 换成 canvas，`#blackhole-layer` 的层序与调参变量不动。 |
| 🟢   | 引力透镜星空（已实现）                | 2026-09-10 完成：`src/components/GravitationalLens.astro` + `#lens-canvas`，仅首页/暗色/桌面（≥769px）；外层极慢 → 中层快 → 内层渐慢，到 `--lens-r-ring` 淡出，**圈内纯黑无星**；吸积盘遮罩为可旋转/压扁/上移的椭圆环 + 边缘羽化；黑洞圈外一圈**圆形小星星带**（`--lens-photon-band` 宽度、`--lens-photon-size` 更小、`--lens-gap-boost` 更亮，且不做切向拉伸）；自带键盘调参工具（反引号 / `?lens=tune`，方向键调每个圈半径/吸积盘/黑洞中心，Shift ×10 / Alt ×0.1，COPY 按钮复制参数）。见 §9 决策 20。 |
| 🟡   | ArtGallery 是死代码 + gallery 双实现   | 详情模板的 art 分支不可达（getStaticPaths 过滤 art）；实际 art 展示由两个冻结的 art 页各自实现的 marquee/lightbox 承担。两套实现并存且 en/zh 版还有细节差异。受冻结政策约束，**动它们需要用户解冻**。 |
| 🟢   | zh/notes 页未用翻译键                  | 已修复（2026-08-14）：改用 `t.sections.notes.*`，zh 翻译描述补句号与 en 对齐。两版样式覆盖的细微漂移仍存在，见 🔴 清单。                                                                              |
| 🟡   | about 页多文件臃肿且实现漂移           | en/zh/nl 三份独立实现（各 ~4000+ 行），canvas 动画脚本三份维护，动画参数与样式有独立差异。重构（共享模板）需要用户同意;这是当前最大的维护成本点。                                                     |
| 🟢   | README.md 是 Astro 模板默认 README     | 已修复（2026-08-14）：重写为面向访客的项目介绍（中英双语，由浅入深）。                                                                                                                                |
| 🟢   | `is:global` 块里的 `:global()` 全线失效 | 已定位并修复（2026-09-17）：`ProjectDetail.astro` 第二个样式块是 `<style is:global>`，Astro 对它不做作用域转换，`:global(...)` 被原样输出、浏览器整条丢弃。受影响的是 V1/V2/V3 对比网格的**全部**样式（含宽度、卡片边框、内边距、标题压缩）和 `article p/ul/ol/li` 三条间距规则 —— 所以 2026-09 那几次"把对比区拉宽"改 `:global(.versions-grid){width:...}` 全都毫无效果（改的是死规则）。修复方式：该块内剥掉所有 `:global()`。**规则：`is:global` 块只写裸选择器。** 复活后暴露一处配套缺陷：黄色竖条 `article h2::before` 的 `top: 30px` 是为正文 h2 的 `padding-top: 30px` 留的，而列内 h2/h3 的 `padding-top` 被压成 0 → 每个卡片里**第二个及以后**的小标题竖条低 30px（`:first-of-type` 那条 `top: 0` 恰好看不出问题）。已补 `.version-column h2/h3::before { top: 0 !important }`。 |
| 🟢   | `.versions-grid` 类名撞车              | 已修复（2026-09-17）：markdown 手写的 `.version-cell` 四列网格（zoem-bike-bakfiets）占用 `versions-grid`，运行时生成的 V1/V2/V3 三列对比网格**正好同名** → 继承 `repeat(4,1fr)`，每列只剩 1/4 宽、右侧还常驻一个空槽（对比区看起来比上面时间线表格窄一截的根因）。运行时那个已改名 `version-compare-grid`（JS + CSS），zoem 页 4×194px 不受影响。 |
| 🟡   | `article` 正文间距规则仍然无效         | `:global(article p){margin-bottom:30px}`、`…ul/ol{30px}`、`…li{14px}` 三条同上被 `is:global` bug 干掉；**即使剥掉 `:global()` 也仍输给作用域版** `article[data-astro-cid] p[data-astro-cid]{margin-bottom:20px}`（0,2,2 > 0,0,2）。当前实际值 = 段落 20px / 列表项 8px，改成 30px/14px 需用户确认（全站详情页都会变）。 |
| 🟢   | og-card 脚本依赖 sharp 传递依赖        | 已修复（2026-08-14）：sharp 声明为 devDependency（^0.35.3），新增 `npm run og-card` 脚本（已运行验证）。                                                                                              |
| 🟢   | `输入/` 与 .gitignore 不一致           | 已解决（2026-08-14）：`git rm -r --cached 输入` 取消跟踪（本地文件保留）。该目录重新定义为**用户给 AI 的投递箱**，永不提交（§12 规则 13）。                                                           |
| 🟡   | Navbar 无当前页高亮                    | 无 `aria-current`/active 样式。属功能缺失而非 bug；加高亮属合理新需求。                                                                                                                               |
| 🟡   | zoem-bike 的 translationKey ≠ 文件夹名 | 文件夹 `zoem-bike-bakfiets`，translationKey 是 `zoem-bike-cargo-box`。配对依赖 translationKey（不依赖文件夹名），所以无功能性影响，但容易让新读者困惑。                                               |
| 🟡   | 无 en→zh 的 `/zh/404` 独立页           | 404 页双语混合固定内容，未按语言适配（可接受）。                                                                                                                                                      |
| 🔵   | 动画时长、断点未 token 化              | 断点因 CSS 限制必须字面量（有 BREAKPOINT REGISTRY 制度）；动画时长字面量是清理时的取舍。                                                                                                              |
| 🔵   | 滚动条隐藏                             | 用户偏好，勿"修复"。                                                                                                                                                                                  |
| 🔵   | `featured` 字段不参与自动筛选          | 首页精选靠硬编码 `featuredKeys`（用户偏好人工策展）。                                                                                                                                                 |
| 🔵   | 卡片/详情无 cover 的 note 条目         | 5 个早期 note 无 cover，卡片与详情做了无封面降级渲染，属正常设计。                                                                                                                                    |
| 🟢   | projects/lab 磁带盒书架（已实现）      | 2026-09-18 完成 + 同日多轮打磨（最新 v1.30）：`src/components/CassetteShelf.astro` 接管六个列表页；CSS 3D（无 three.js 依赖）、开场左→右升架 + 中心抽出转 15°、一屏九盘且边缘不跳变、封面铺满盒面、`INSPECT FILE` 实心按钮、**拖拽＝落点即整段行程（最多 `DRAG_MAX 5` 格）+ 临界阻尼滑行 + 落在布景里时用 `snap` 滑回最近真磁带**、右侧简介面板（WIP 琥珀/ONLINE 绿）、标签筛选只重排不黑屏、换语言续看当前盘、换盘一条缓动 tween（旋转与 `progress` 无关）、两端单折线 miter 尖角的粗箭头（同相向外）；手机端退化为平面卡片。见 §9 决策 22b、v1.18/v1.22/v1.30 三条记录。 |
| 🟡   | 磁带盒「厚度感」受几何限制             | 主角只有 15° + 外壳深度 **0.37 世界单位（≈7.6% 宽，v1.16 由 0.70 调薄）**：一屏九盘的盘距（`SPREAD_X 2.2`）下更厚的外壳会互相穿插，所以左侧壁/底部的投影只有十几像素——靠**面明暗差 + 边缘高光 + 外投影 + 暖色键光**去撑立体感。想更立体只能加大 `CAS_D` 或 `RX_BASE`，但会同时改变「磁带」的比例并挤到邻盘；用户若要更强的 3D，先确认愿意牺牲盘数与比例。 |
| 🟡   | 一屏九盘 = 每盘必然变小                | 九盘铺满 1600px 宽时主角约占画面宽 **28%**（v1.18 定的构图，对齐 `步骤4.png`）。这是几何约束：`SPREAD_X` 与 `MIN_WORLD_W` 是**同一个决定的两个常数**（`2.2` / `19`），前者的 pitch 决定主角多大、后者决定每盘多大，判据是「这时还有几张是完整的」。想更大只能减盘数（`MAX_SLOTS 4 → 3` 回到七盘）。 |
| 🔴   | 可选重构清单                           | ArtGallery 与 art 页 gallery 实现统一（需解冻）；about 页双语言共享模板化；zh/notes 与 en 版样式漂移收敛。**这些都需要用户同意，不要擅自执行。**                                                      |

已解决的历史问题（勿回退）：详情路由 404（`/notes/{slug}` 发散映射，靠 `routes.ts` 单一来源解决）；TechArticle 缺 `dateModified`/日期无时区（2026-08 修复）；`translationOfWork` 嵌套字段不全（2026-08 补全）；Person 实体补 `alternateName: RrSuika`；2026-08 清理删除的遗留文件（旧 `src/content/config.ts`、`ContentList.astro`、空 `retro-ui.css`/`variables.css`）;**不要再创建同名遗留文件**。

---

## 11. 快速索引："我要改什么 → 去哪"

| 需求                         | 优先检查位置                                                                                                            |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| 改首页 Hero / 恢复终端       | `src/components/home/Hero.astro` + `src/styles/global.css` §14（删除隐藏规则）                                          |
| 改背景星空 / 恢复神经网络      | `src/layouts/Layout.astro`（`#starfield-canvas` + 3D 星空脚本；神经网络恢复设 `data-neural="enabled"`）+ `src/styles/global.css` §10 |
| 改首页黑洞视频 / 位置大小      | `src/styles/global.css` §10 `#blackhole-layer` 里的 `--blackhole-*` 变量（x/width/height/scale/opacity 是固定值，**y 是 `calc(122px + 37.8vh)`，按用户实测两点拟合**）+ `#blackhole-video` 的 `object-fit`；换视频改 `public/media/blackhole.mp4`（markup 里是 `data-src`，换路径改 Layout.astro）；关掉黑洞=去掉三个首页 `<Layout>` 的 `blackhole` prop；手机端开关=Layout 脚本的 `min-width: 769px` 与 global.css §10 同名媒体查询 |
| 改引力透镜 / 调每个圈的位置    | `src/components/GravitationalLens.astro`（分层/速度/透镜数学）+ `src/styles/global.css` §10 `#lens-canvas` 的 `--lens-*` 变量；现场调参：导航栏齿轮图标 / 反引号键 / `?lens=tune`（方向键调每个圈半径/吸积盘/中心，Shift ×10 / Alt ×0.1，COPY 按钮复制参数）；§10 默认值与组件内 `FALLBACK`/`SCALED` 互为镜像（视口自适应基线，改一处须两处同步，见决策 21）；关掉透镜=去掉三个首页的 `blackhole` prop |
| 改首页精选项目               | `src/pages/index.astro`、`zh/index.astro`、`nl/index.astro` 的 `featuredKeys`                                           |
| 改首页 SYS.LOG 规则          | `src/pages/index.astro`（zh 同）frontmatter `latestEntries` 逻辑                                                        |
| 改导航 / 语言切换 / 主题按钮 | `src/components/Navbar.astro`（按钮）+ `src/scripts/theme-transition.ts`（揭示动画）+ `global.css §15`。⚠️ 主题揭示是**整页拷贝的圆圈波**（见决策 23）：`.theme-stack` 挂在 `<html>` 下（不能挂 `<body>`，否则下一次克隆会把 stack 一起克隆）、每份拷贝带 `data-theme` + `.theme-scope`、`buildScope()` 在运行时把页面所有 `:root…` 规则改写成 `.theme-scope…`（**裸 `:root` 也要，深色 token 就在那**）。⚠️ 连点期间根停在 `dark`（`NEUTRAL_THEME`），落地才 `applyInstant()` 落到目标主题并**关掉过渡**；⚠️ 不要在拷贝里 pause 动画（第 0 帧），不要给拷贝加 `overflow:hidden`/`contain:paint`（可能压平克隆体里的 3D）。⚠️ **拷贝的构建放在 `pointerdown` 预热、点击只做搬运**（点击里 layout ~700 节点 = 101ms 顿帧，见决策 23），圆的 tween 先 pause 一帧再 play。改完必须复验：拷贝 vs 同主题实时渲染逐像素 |Δ| ≤2、圆内是目标主题/圆外是旧主题、任意一帧都不是纯色、落地帧 body 背景已到位且没有在飞的过渡、**点击同步耗时 <12ms**。⚠️ **落地到暗色时背景要渐入**：浅色下两个 canvas 是 `display:none`，过渡救不了它（没有可插值的起点），所以由 `applyInstant()` 在换主题**后一帧**给 `#starfield-canvas`/`#lens-canvas` 加 `.sf-arrive`（`global.css §10` 的 `sfArrive` 关键帧，1.05s，**只动 opacity、绝不动 transform**——那两个 canvas 有 `position: fixed` 的子元素，transform 会让它们变成包含块）、`ARRIVE_MS 1100` 后摘掉（已在身上的 class 无法重放自己的动画）；⚠️ 它必须在 `applyInstant` 而不是 `setRoot`（连点期间根停在 `NEUTRAL_THEME`，`setRoot` 触发时页面还被拷贝盖着，渐入会在拷贝后面烧掉） |
| 改站点开场 / 预加载 | `Layout.astro`「SITE INTRO GUARD」（`<head>` 内联脚本，决定放不放 + 拥有 `window.__rrsIntroGate`）+ `src/scripts/site-intro.ts`（进度模型与揭幕）+ `global.css §16`。⚠️ **判定必须在 head**：模块脚本跑得太晚，站内跳转会先闪封面再撤销；三态是「会话首个页面放 / F5 放 / 站内跳转不放」。⚠️ **进度是「快速 0→100，早到就快进」**：计数走参考原曲线 `max(t, easeOutCubic(t)·0.85)`，`COUNT_MS 2200` 内**真的到 100**（实测 49% @0.55s、74% @1.1s、100% @2.2s）；页面提前就绪时余量**快进**而不是跳变（`SETTLE_MS 260`，且不早于 `MIN_MS 1400` 落地）；计数走完而页面未就绪时**停在 100**，由状态行说「Waiting on assets」；状态阶梯（Initialising → Loading assets → Calibrating layers）由**计数**驱动而不是由加载驱动。`OPTIONAL_MS 2200`（视频/字体）与 `MAX_MS 3200`（其余）分开。⚠️ **退场是信号色扫描**：满屏色板从底边长上来（`scaleY(0→1)` 620ms）→ 停 280ms（读数依次淡出）→ `opacity→0` + `blur(7px)` 720ms 消散；⚠️ **色板满高那一帧要把底板/舞台/轨道一并隐藏**（`.is-revealing`），否则消散落到底板上、下一帧底板再被切掉。⚠️ 所有入场动画必须用 `animation-fill-mode: backwards`——用 `both`/`forwards` 时填满的末帧会**压过**普通声明，`.is-sweeping` 的淡出会静默失效。⚠️ 风格是**机能仪表盘**：参考里只有底板/网格/量表/`[NNN]` 计数/四角标签/状态行被搬过来，**扫描线与色差阴影刻意不搬**，信号色取当前 `--accent`。⚠️ 任何改动都要保住三条兜底：guard 的 6s 自开、`<noscript>` 隐藏、skip 时删节点；并且 `__rrsIntroResolve` 必须被调用（书架在等它，否则书架入场永不播放）。⚠️ 探针必须用 `Page.reload`（`navigation.type === "reload"` 才播），`Page.navigate` 会被当成站内跳转跳过 |
| 改页脚链接/版权              | `src/components/Footer.astro`                                                                                           |
| 改项目/实验列表（磁带盒书架） | `src/components/CassetteShelf.astro` —— 六个列表页共用：姿态数学在 `<script>` 顶部「WORLD CONSTANTS / POSE」（1:1 移植参考 `layoutPose()`）、磁带盒外观在 `<style>` 的 `.cas*`、空磁带布景在外壳的 `[data-fillers]`、文案在 `translations.ts` 的 `shelf` 段。⚠️ **preserve-3d 卫生**：`.cas-f` 面的任何祖先都不能有 `overflow:hidden`/`opacity<1`/`filter`/`backdrop-filter`/`clip-path`/`mask`/非 3D `transform`，否则整盒塌成平面卡；逐盘淡入用 `--cas-o`（加在**面**上）；⚠️ **除环进出与开场外，磁带上的任何东西都不许用 opacity 淡出**——`--cas-front-o` 已随 v1.24 删除，封面淡到 0.8 会让邻盘互相透视。⚠️ 主角转 `+15°`（露**左**侧壁；参考的 `−15°` 露的是右侧）。⚠️ **位置只有一个真相**：`slotIndex(tape) = ring − current + progress`——绘制、淡入淡出、DOM 排序都读它，别再派生第二份。⚠️ **居中取决于 `current = clamp(half, walkFrom, walkTo)`**，把选中盘放在「第一张真磁带」上会让整行左移半屏。⚠️ `casEls` 要用 `.cas:not([data-fillers] .cas)` 排除空盘。⚠️ **箭头方向只看路径数据**：每条是**一条描边折线**，**尖＝中间那个坐标**（prev `M12 2 L2 13 L12 24` 尖在左、next `M2 2 L2 13 L2 24` 尖在右），不要再引入 `scaleX(-1)` 镜像（镜像元素与 Q 弹动画会抢同一个 transform，且镜像做不出尖角）；尖角靠 `stroke-linejoin: miter`，米特尖溢出 14px 框约 1px，所以 svg 必须 `overflow: visible`。两套关键帧是刻意的（`shelfBounceOut` / `shelfBounceOutLeft`）：`translate` 取反、`scale` 逐字照抄，**不要用 `animation-direction: reverse` 合并**（时间镜像 ≠ 空间取反，且会抹掉回缩/外推的粗细对比）；bounce 挂在内层 `svg`，`.shelf-arrow` 上不挂任何 transform。调参入口：`SPREAD_X` 与 `MIN_WORLD_W`（**必须一起改**：pitch 决定主角多大/两侧留多少，帧宽决定每盘多大——判据是「1600px 下还有几张是完整的」）/ `WORLD_H` / `CAS_D` / `RY_CENTER`·`RX_BASE` / `MAX_SLOTS`（绘制位数）/ `RISE_*`·`PULL_DUR`（开场）/ `DRAG_MAX`·`SLOT_DRAG()`·`FLICK_COMMIT`（拖动：**行程**上限、一格多少 px、同向加一格的甩动阈值）。⚠️ **改拖动前必须知道的那几条**：`progress` 随指针向右**增大**，所以正中那盘是 `current − progress`，落点是 **`dest = current − land`**（写成 `+ land` 既选错盘又把整行推 `2·land` 格）；别把 `current` 中途归一化（会把行程夹回一格）；环长必须 `≥ n + 2·(span + DRAG_MAX)`（加 `DRAG_MAX` 不加环长＝绘制带走出环尾）；`snap.to` 与 `recycle` 的符号相反（滑动归位的 tween 必须结束在 `−step`）；`recycleFromSpring` 的余量是 `round(spring.target) + step`；甩动加票只许把取整补到运动方向的下一个整格（`ceil`/`floor`），不是无条件 `±1`；`recycle` 的 re-base 区间是**可走区间 ±1 格布景**（用环端会让归位的那一步被拒、`lock` 永不释放）；落点之间的差值必须**滑**而不是赋值（赋值＝整行一帧横跳）。**容器/主题/箭头改动后必须复验**：3D 没塌、主角居中、箭头指向（墨迹图判据：人字只有一列是**单段**——那是尖，另一头必是两段＝两条尾巴，这个不对称就是方向；`getBoundingClientRect`/`getComputedStyle` 判不出来）、两箭头同相向外（轮询 `getComputedStyle(svg).translate`，⚠️ `display:none` 的一侧根本不跑动画）、空盘不参与计数。见决策 22b 的 CDP 验证法 |
| 改项目/实验卡片（首页精选、notes） | `src/components/ProjectCard.astro`（`.card` 的 `backdrop-filter: blur(20px)`；⚠️ 它**不能**和 `-webkit-backdrop-filter` 写进同一条规则——构建期 lightningcss 会按别名合并、只留最后一条（`-webkit` 在后则生产站整条失效），Safari 兜底因此放在紧随的 `@supports` 独立规则里，见决策 22；⚠️ `main`/`.card` 入场动画必须用 `animation-fill-mode: backwards`，残留 transform 会让 Chrome 丢掉模糊）                                                                                      |
| 改详情页排版/TOC/灯箱        | `src/components/ProjectDetail.astro`                                                                                    |
| 改滚动进度条                 | `src/components/ScrollMeter.astro`                                                                                      |
| 改列表筛选标签               | 各列表页 frontmatter `filterTags` + global.css §12（`.tag-bar/.tag-chip`）                                              |
| 新增项目/笔记/实验           | `src/content/entries/`（§4.5 步骤）；首页精选另加 `featuredKeys`                                                        |
| 改内容字段/Schema            | `src/content.config.ts`（改 enum 会牵连路由，见 §3.3）                                                                  |
| 改全局颜色/间距/圆角/字体    | `src/styles/global.css`（token 在 §1/§2；遵守 fidelity 政策）                                                           |
| 改某条文案                   | `src/utils/translations.ts`（en+zh 两处）                                                                               |
| 改 URL 映射                  | `src/utils/routes.ts`（`typeToRoute`/`buildEntryUrl`）＋同步 ProjectCard                                                |
| 看/调磁带盒书架的 3D        | CDP 验证法（决策 22b）：headless Chrome 用 `--virtual-time-budget` 会饿死 rAF、拍不到 transform；改用 `--remote-debugging-port` + 页面内 `Runtime.evaluate` 注入探针，用 `document.elementsFromPoint` 网格扫描打印「哪张面画在哪」的 ASCII 图，一眼看出 3D 是否塌成平面。**看形状要解码像素**（v1.21/v1.22 两轮箭头都靠它定案）：`Page.captureScreenshot` 取小 clip → 自己解 PNG（`zlib.inflateSync` + 逐行反滤波）→ 打印墨迹图/列剖面。⚠️ 墨迹要取**两次差分求交**（画面→隐藏目标→画面），否则背景星空在两拍之间漂移，会把移动的星当成墨迹（先隐藏 `canvas` 更省事）。⚠️ 动画值不能靠截图采样（有 8–20 帧延迟），直接轮询 `getComputedStyle(el).translate` / `.clipPath` |
| ⚠️ 折线/路径的「尖」在哪     | **折线的尖端是中间那个坐标，不是第一个**：`M2 2 L12 13 L2 24` 画的是 `>`（顶点 (12,13)）。按首坐标推方向会把一对箭头装反（v1.22 的真实事故）。判据用像素：人字只有**一列是单段**（尖），另一头必是两段（两条圆帽尾巴） |
| 改语言判定/路径换算          | `src/utils/i18n.ts`                                                                                                     |
| 改图片解析                   | `src/utils/images.ts`                                                                                                   |
| 改 head/SEO/hreflang/JSON-LD | `src/layouts/Layout.astro`（详情 JSON-LD 在两张详情模板里；Person 在 about 页）                                         |
| 改 sitemap                   | `src/pages/sitemap.xml.ts`                                                                                              |
| 改 robots                    | `public/robots.txt`                                                                                                     |
| 改 404                       | `src/pages/404.astro`                                                                                                   |
| 改分享卡                     | `scripts/og-card-gen.mjs`（改后 `npm run og-card` 重跑）+ `public/og-card.png`                                          |
| 改 art 展示                  | ⚠️ 冻结区：`src/pages/art/index.astro`、`zh/art/index.astro`、`ArtGallery.astro`、`gallery` 字段;仅允许 SEO frontmatter |
| 改 about 页                  | `src/pages/about/index.astro` + `src/pages/zh/about/index.astro`（双文件都要改）                                        |
| 构建/部署问题                | `package.json`、`astro.config.mjs`；Cloudflare 控制台（仓库内无部署配置）                                               |

---

## 12. AI 工作规则

1. **执行任务前，先读本文件**（AI_CONTEXT.md），再用 §11 快速索引定位相关文件。
2. **不要无差别扫描整个项目**;只阅读任务涉及的源文件；仅当任务涉及本文档未覆盖的内容，或怀疑文档过时，才扩展阅读范围。
3. **CLAUDE.md 的约束是硬规则**（设计系统 token 政策、冻结区、路由单一来源、SEO 约定），与本文件冲突时以代码和 CLAUDE.md 为准。
4. **复用优先**：已有组件、已有样式模式（`.tag-chip`、`.data-tag`、卡片/详情）、已有工具函数，不要重复造。特别是：不要在组件里自建路由映射（历史 404 教训）。
5. **不要擅自改变**：路由结构、Content schema 的 `type` 枚举、组件结构、设计语言（含旧调色板字面量）。结构性改动先与用户确认。
6. **冻结区**（art 页面 4 处 + gallery 字段）：只允许改 SEO frontmatter。
7. **双语言义务**：任何 en 页面/文案改动都要同步 zh 镜像文件与 `translations.ts` 两语言。新静态页面必须建 zh 镜像。
8. **发现代码与本文档不一致**：以当前代码为准完成任务，并在任务收尾时更新本文档。
9. **完成较大架构修改后，主动更新本文档**（目录树、路由、组件职责、决策记录、技术债状态）。
10. **敏感信息禁入本文档**：不写密码、Token、API Key；调试临时代码的痕迹也不写入。
11. 开发服务器按 AGENTS.md 用后台模式（`astro dev --background`）；验证用 `npm run build`。
12. **用户的新约定必须写进本文件**：用户在对话中传达的任何新规则、新决策、新流程（例："`输入/` 是给 AI 的投递箱"），执行任务后要主动写入本文件（必要时同步 CLAUDE.md 和记忆），让未来的 AI 无需用户重复提醒。
13. **`输入/` 投递箱工作流**：用户会把新素材（项目图片、文档、skill 包等）放进 `输入/`。AI 应主动读取其中内容并转化为站内产物（写入 `src/content/entries/` 或合适位置），完成后与用户确认是否清理原文件。该目录**永不提交/上传 GitHub**（已移出版本控制）。
14. **荷兰语文风约定（2026-08-16 用户指示，改写 nl 文案必须遵守）**：nl 文案要像生活在荷兰的设计师/工程师本人写的;自然、偏口语，但整体专业、克制（参考技术博客/作品集语气）；避免过度书面化/生僻词、AI 腔（句式过于工整、重复表达、泛化术语）、机翻措辞。技术/设计/互联网词汇按荷兰从业者习惯**保留英文**（prototype、workflow、embedded systems、feedback、interface、rendering、hardware、software 等），不强行纯荷兰语，也不刻意插英文。保持原意、信息结构、技术含义与语气，不增删内容；原文已自然则不改。**硬约束**：nl 列表页（projects/lab/notes）的 `filterTags` 必须与条目 nl.md 的 `tags` 值**逐字一致**;筛选靠精确字符串匹配，改词会导致 chip 失效；art 页面与 art 条目冻结（§12 规则 6）不适用本条。
15. **英文文风约定（2026-08-16 用户指示，改写 en 文案必须遵守）**：en 文案要像设计师/工程师本人写的;自然、口语化、像真人，同时专业、略正式（技术博客/作品集语气）；清晰、自信、务实，不企业腔、不过度打磨。避免过于学术/正式的词、AI 腔（句式过于工整、重复开头、营销泛词、过度抛光）、机翻痕迹。适当简洁直接。技术/设计/软件/电子/AI/网络术语用从业者自然使用的说法。不增删内容、不改观点；已自然的句子不动。**硬约束同上**：en 列表页 `filterTags` 与条目 en.md `tags` 逐字一致；en.md 的 `tags` 字段禁止改动（B/C 两批规则）；art 冻结区不适用。**已知遗留**（用户未要求处理）：en 内容英式/美式拼写混杂（colour/color、organise、realised 等），各文件保持原有拼写。
16. **中文文风约定（2026-08-16 用户指示，改写 zh 文案必须遵守）**：zh 文案要像真正做工业设计/嵌入式/视觉创作的人自己写的;自然、口语化、像真人；避免生硬、机械、翻译腔。偏正式、专业但不书面化（技术博客/设计师个人网站语气）。**禁止**官方宣传稿/公关腔："致力于""赋能""探索……可能性""打造……体验"类套话一律不出现。简洁直接、符合现代中文互联网语境。减少 AI 特征（过度工整句式、连续相似结构、无意义总结、堆砌术语）。可自然夹杂英文术语（prototype、workflow、embedded systems、AI、ESP32 等），不强行翻译也不刻意插英文。项目/实验记录保留个人表达与思考感，但不过度随意。保持原意、信息结构、技术含义、核心观点；不增删信息；已自然的句子不动。**用户亲笔保留**：zh 版 `annex.intro`（"我超爱复古未来主义！！…"）逐字保留、永远不润色（zh about 页内还有一份硬编码副本，同样保留）。**硬约束同上**：zh 列表页 `filterTags` 与条目 cn.md `tags` 逐字一致；cn.md 的 `tags` 字段禁止改动；art 冻结区不适用。**备注**：zh about 页有部分 annex 文案的页面内硬编码副本（与 translations.ts zh 重复）。
17. **lieflat-charts skill（2026-09-01 安装）**：模板驱动的数据可视化/HTML 报告生成 skill，源码在 `Skill/lieflat-charts/`（入口 `SKILL.md`，含 catalog.md、templates/、color-presets.js 等）。发现路径：`.claude/skills/lieflat-charts` 是指向它的 junction（`.claude/` 已 gitignore，不随站点部署）。嵌套 .git 已移除，更新方式为重新 clone 覆盖该目录。用户要求用它做网站优化（图表/报告类内容）；绘制图表/生成报告前应加载此 skill，遵循其"默认图表模式、模板优先、Mono 保底配色"规则，产出与站点现有设计语言冲突时以站点设计系统（CLAUDE.md）为准。
18. **去AI味 skill（lieflat-less-ai-tone，2026-09-01 注册）**：白名单式改写写作中的 AI 痕迹——只能处理 skill 规则清单内的问题，未命中规则的文字逐字保留，不改文章框架、不增删信息。**原稿**在 `Skill/去AI味 skill.md`（用户自己维护）；**注册副本**在 `.claude/skills/lieflat-less-ai-tone/SKILL.md`（Claude Code 发现路径）。改动原稿后需重新复制到注册路径同步。适用于站点文案（en/zh/nl）成稿后的去 AI 味清理，与本文件 §12 规则 14/15/16 的文风约定配合使用。
19. **改写严禁添加不存在的细节（2026-09-01 用户指示，最高优先）**：去 AI 味/改写任何文案时，禁止添加原文没有的事实、技术细节或因果。曾有教训：AI 虚构"用单片机 PWM 驱动灯带"，实际用户用的是 PWM 控制器。硬件方案、器件型号、连接方式等拿不准的细节以原文为准；原文模糊就保持模糊，需要澄清时先问用户，绝不自己补写。
20. **各语言本土化优先（2026-09-01 用户指示）**：改写任何文案时，每个语言都要用该语言的本土化表述，不能直接从另一个语言润色好的版本机翻过来。发现原文带有机翻痕迹（语序照搬、直译习语、不自然的固定搭配）时应改为该语言的母语者自然说法，同时遵守规则 19（不增删事实）。与 §12 规则 14/15/16 的文风约定配合。
21. **中文翻案腔零容忍、拒绝夸张、真正润色（2026-09-01 用户指示，重要）**：① 中文文案里"不是……而是……"式翻案腔要彻底消灭——包括"不只是 A，还有 B""并不是 A""A，而不是 B"等变体，一律改成直接陈述（例："目的不是为了炫技，而是验证……"→"目的是验证……"；"这是一件背心，不是道具也不是复刻"→"这是一件背心"）。事实性否定（"有些板子不会自动进入烧录模式""如果光本身就不均匀"）和代码注释不受影响。② 收敛夸张用词（用户原话："我不会写'打个喷嚏就掀起来'这种夸张"），改为平实描述。③ 去 AI 味不等于只调标点/语序——要真正润色表述方式，让文本像人写的。2026-09-01 已按此对全部非 art、非首页的中文内容做深度清扫。
22. **英文/荷兰文 AI 味标准（2026-09-01 网络调研 + 用户指示）**：按公开调研确定 en/nl 的 AI 写作痕迹清单。**en**：负面平行结构（"not just X, but Y"、"isn't X — it's Y"、"Not because A, but because B"）一律改直接陈述；模板短语（"Furthermore/Moreover"、"It's important to note"、"plays a significant role"）不得出现；过度修饰词（delve/leverage/utilize/crucial/robust/seamless/landscape/tapestry 等）在技术语境可用、修辞语境禁用；破折号密度偏高是公认标志（本站在口语化随笔中保留，揭晓式破折号已随负面平行结构一并清除）。**nl**："niet alleen… maar ook…"、"niet X, maar Y"、"Geen X, maar Y" 同规则处理；句首连接词 echter/bovendien/daarnaast 与古旧词 voorts/derhalve 避免；"het is belangrijk om te benadrukken" 类模板句不得出现。保留项：事实性否定、规格注记（"allowed, not required"）、代码注释、口语化片语（"Not bad for a $5 microcontroller"）。2026-09-01 已按此清扫 en/nl 全部非 art、非首页内容。
23. **条目正文不用破折号（2026-09-03 用户指示）**：太阳能项目文章（home-solar-storage-design，en/cn/nl 三语）中的 ——/— 用户明确不想看到，已全部替换为逗号、冒号或分号（数字区间 – 如 92–95% 保留）。后续新写条目正文避免使用 ——/—，需插入补充说明时优先逗号、冒号或括号。
